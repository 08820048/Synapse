# Synapse 开发进度

> 最后更新：2026-08-14（补齐斜杠浮层动画、菜单图标与内部笔记链接跳转）
> 当前阶段：Markdown 编辑效率 — 斜杠命令已具备完整原生交互反馈，Vault 内笔记链接可从渲染态直接打开
> 同步规则：每次功能调整后立即维护本文件；每轮开发结束前再次核对代码、测试与本文档一致性。

## 当前可用功能

- Rust + GPUI 原生桌面应用可启动。
- 已建立统一的原生通知系统：通知通过 gpui-component `Root` 的右上角队列呈现，正文使用官方 `Alert` Info / Success / Warning / Error 变体，可手动关闭并在 5 秒后自动消失；透明通知外壳避免重复容器视觉，保留组件既有进出动画和最多 10 条可见队列限制。
- 用户直接触发的文件/文件夹移到废纸篓、当前笔记删除、待办/书签删除、待办/书签标签删除、标签关联移除及批量清除完成项，已统一接入 gpui-component 确认对话框：遮罩不可点击关闭、Escape 取消、Enter 确认，确认按钮使用 Danger 变体；取消不修改数据，成功或失败都会显示双语结果通知。
- 待办与书签危险操作持久化失败时会恢复操作前的内存快照，避免界面显示已删除但磁盘保存失败；对象在确认期间被其他操作移除时显示错误通知且应用继续运行。用户主动开启的待办自动清理保持既有 420ms 完成态停留与 220ms 右移淡出，不为每条自动删除重复弹框或刷通知。
- 原生工作区选择流程已接入结果通知：成功切换后显示当前路径，目录打开失败、选择器失败或异常关闭时显示错误 Alert；用户取消选择不产生误导通知。通知与确认框标题、说明、按钮文案全部跟随简体中文 / English 全局设置。
- 当前 Vault 已接入 `notify 7.0.0` 的平台原生递归文件系统监听；Finder、终端、下载器、同步工具等外部来源在 Vault 内新建、复制、移动、重命名或删除文件夹/Markdown 文件后，会自动刷新侧栏文件树。
- 文件系统事件使用 180ms trailing debounce 合并事件风暴；纯访问事件不会触发扫描。监听器用 Vault generation 隔离，切换 Vault 会销毁旧 watcher，旧目录的延迟事件无法刷新新目录。
- 外部刷新只调用 `ShellState::refresh_vault_entries` 替换目录与笔记快照，不重新打开任何文档，不覆盖未保存 Rope 缓冲区、光标或脏状态；刷新后只清除已经不存在目录的折叠状态，仍存在目录保持原展开/收起选择。
- 侧边栏待办与书签集合入口不再使用独立的 8px 左内边距，改为与根级文件夹、笔记及空目录占位共用 `SIDEBAR_TREE_ROOT_INSET = 12px` 对齐基准；两类集合图标向右校准 4px，并保持文字、右侧展开按钮和 40px 命中区不变。
- 待办和书签标签栏已统一为参考项目 TagRail 的视觉层级：40px 行高、2px 行距、13px 标签文字、11.5px 右对齐计数、9px 色点；未选中标签为 muted、选中为 ink，计数在 faint/muted 间同步切换。
- 书签标签选中态不再给各行静态背景，而是直接复用待办的唯一 `theme.active` pill 与 Markd 同源 360/32/0.6 过阻尼响应，在“全部”和具体标签之间执行 180ms 可中断纵向滑动。
- 待办和书签的具体标签行均在悬停时使用统一 `theme.hover` 背景，并在最右侧将计数淡出、Lucide `x` 移除入口显现；图标使用绝对定位的 40px 独立命中区，显示时不改变标签文本或计数布局，也不会与计数重叠。删除标签会清理所有项目的对应标签关系并立即持久化，但不会删除待办或书签本身。
- 侧边栏“书签”已从占位入口升级为完整集合：左侧图标和名称只打开书签工作区，右侧 Lucide `plus` / `minus` 只控制下方完整列表，展开列表显示第一标签色圆点和省略号标题，点击直接交给系统浏览器打开。
- 独立书签工作区占用编辑器区域，工具栏显示“书签”、Markdown“导出”和“新建标签”；主体使用 940px 居中内容页、168px 标签筛选栏以及右侧搜索/添加与列表区域。
- 书签输入框支持中文搜索和 URL 添加双重语义：普通文本按标题、URL、标签实时过滤，裸域名和 HTTP(S) URL 回车后规范化保存；空值、非法协议和重复 URL 显示中文错误，新记录置顶并继承当前筛选标签。
- 保存书签后通过应用既有 Rust HTTP 客户端异步抓取 Open Graph 标题/图片、网页 title 和 favicon；请求不依赖 WebView 或 JavaScript，不在 GPUI 主线程阻塞，网络失败保留可用书签并停止无限重试。
- 书签条目显示 56×40 预览图，按 Open Graph 图片、favicon、Lucide `globe` 逐级降级；点击使用系统浏览器打开，双击标题原位编辑，悬停提供标签多选、复制 URL 和删除。
- 书签标签栏提供“全部”、主题色圆点、选中状态和真实使用计数；标签支持中文创建、空值/重名/48 字符校验、删除，分配面板支持多选，已分配关系以可单独取消的 `#标签` 胶囊显示。
- 书签导出通过系统保存面板生成包含标题、URL 和标签的 Markdown；标签注册表、书签稳定 ID、标题、预览、favicon、抓取状态、创建时间和多标签关系写入平台用户配置目录的 `Synapse/bookmarks`，长度编码会转义制表符、换行和反斜杠，损坏记录跳过。
- 书签侧栏列表按动画规约执行 150ms 高度/透明度过渡；新建标签沿用 180ms 宽度和 140ms 透明度交叉切换。新增官方 Lucide `globe` SVG 作为书签预览降级图标。
- 无参数启动的默认窗口尺寸为 1809 × 1332，并在可用屏幕区域居中；最小尺寸继续保持 900 × 560。
- 无参数启动时可点击空状态或侧边栏的 `Open Vault`，通过系统原生文件夹选择器打开本地 Vault。
- 仍支持通过命令行参数直接指定初始 Vault。
- 文件树递归发现 `.md` / `.MD` 文件和空文件夹，并按相对路径稳定排序。
- 文件夹支持点击展开/收起：展开态显示 Lucide `folder-open`，收起态显示 `folder`，收起后隐藏全部后代条目。
- 展开的空文件夹会在下一层显示浅色“空文件夹”占位。
- 扫描过程跳过符号链接和非 Markdown 文件。
- Vault 打开与扫描失败会返回类型化错误，不会导致核心逻辑 panic。
- 默认界面已收敛为左侧导航与中央 Markdown 编辑器，不再显示 Backlinks 右侧栏。
- 可点击文件树中的笔记并将精确 Markdown 内容加载到中心编辑区。
- 活动笔记使用 Rope 缓冲区并接入 GPUI 原生输入协议，支持中文 IME、组合文本、换行、Backspace/Delete 和上下左右/Home/End 导航。
- 编辑器使用真实 shaped-line 布局处理鼠标点击，光标会定位到最近的 Unicode 字符边界；上下移动经过短行后仍保留期望列。
- 普通 Enter 已接入 `writ 0.18.1` tree-sitter 列表上下文：支持无序/有序/任务列表与引用续写、序号递增和空列表退出；Shift+Enter 保留原始换行。
- 默认编辑模式输入 `/` 会在真实光标下方打开纯 Rust + GPUI 斜杠指令面板；支持继续输入中英文名称/关键字过滤、上下键循环选择、Enter/Tab 执行、Escape 关闭、鼠标悬停同步选中和点击执行。菜单会根据窗口剩余空间自动显示在光标上方或下方，并限制在窗口 12px 安全边距内；打开和关闭均使用可中断淡入/淡出位移动画，所有菜单项的 Lucide 图标在名称左侧以显式主题色稳定显示。
- 斜杠指令已覆盖与参考项目一致的 12 项能力：链接到笔记、正文、一级/二级/三级标题、无序列表、有序列表、任务列表、引用、代码块、分割线和 3×3 表格；执行时会移除 `/查询内容` 并原位转换当前 Markdown 块，代码块光标进入内容区，表格光标进入首个单元格。
- “链接到笔记”会切换到独立原生搜索选择器，排除当前笔记并按标题和文件夹路径过滤最多 8 条候选；选择后插入 `[标题](Vault相对路径.md)` 标准 Markdown 链接和尾随空格，中文、空格及特殊路径使用 UTF-8 百分号编码，文件继续保持可移植而非写入私有节点格式。默认渲染态单击这类内部链接会校验当前 Vault 目标并直接打开对应笔记页签，外部地址、越界路径和缺失目标不会被误当作内部笔记。
- 编辑器新增平台标准格式快捷键：`Cmd/Ctrl+B` 粗体、`Cmd/Ctrl+I` 斜体、`Cmd/Ctrl+U` 下划线、`Cmd/Ctrl+Shift+S` 删除线、`Cmd/Ctrl+E` 行内代码、`Cmd+Option+C` / `Ctrl+Alt+C` 围栏代码块。非空选区复用划词工具条的 Unicode 安全包裹/取消逻辑；空光标插入成对 Markdown 标记并停在中间，代码块快捷键可包裹完整选中行或插入空代码块。
- 行首输入 `````rust``、`~~~typescript` 等至少三个同类围栏及可选单词语言标识后按 Enter，会自动插入空内容行、补齐同长度/同缩进闭合围栏并把光标留在内容行；下方已有匹配闭合围栏时只补内容行。普通段落内的反引号、少于三个标记、超过三级缩进和包含空白的无效语言标识继续执行普通换行。
- 任务列表在阅读状态使用 16px GPUI 原生复选框替换 `[ ]` / `[x]` 源码标记，支持嵌套任务与大小写 `X`；点击复选框会直接切换标准 Markdown 标记、保持原光标位置并更新脏状态。
- 编辑光标以约 530ms 周期闪烁；输入、点击或方向移动会立即恢复可见并使旧计时任务失效。光标宽度由 1px 提升为居中的 1.5px，颜色直接使用 Ornata 同源的完整 ink/foreground（浅色 `#191919`、深色 `#ebebe8`），不再呈现细弱浅线。
- 编辑器具备锚点/活动端选区：支持鼠标跨行拖选、Shift + Left/Right/Up/Down/Home/End 扩展选择，并绘制选区背景。
- 鼠标或 Shift 键盘形成非空选区后，会在选区上方 8px 自动显示纯 Rust + GPUI 划词工具条；拖选过程中隐藏，释放后显示，跨行与软换行选区使用真实 shaped-line 坐标定位，窗口边缘自动限制在 12px 安全区内。
- 划词工具条按参考项目使用 32px 高、8px 圆角、2px 内边距和阴影浮层；最左侧为带 Lucide `sparkles` 的反色 `Ask AI`，其后是 28×28 的粗体、斜体、下划线、删除线、行内代码和链接按钮，激活状态反色并保留 Tooltip。
- 划词工具条不再依赖 GPUI Component 按钮的隐式 `currentColor` 继承：`Ask AI` 文本和 Sparkles、六个格式/链接图标、链接确认文本及 AI 发送图标均使用显式主题前景色，浮层自身也设置 `popover_foreground` 兜底；`Ask AI` 内容禁止 flex 压缩，深浅主题和按钮状态下均不会再与背景同色或被挤没。
- 粗体、斜体、删除线、行内代码分别写入标准 Markdown 标记，下划线使用可移植的 `<u>…</u>`；再次点击可移除格式。包裹与取消均保持原正文选区，支持中文和其他 Unicode 文本，并避免把粗体 `**` 错判成斜体 `*`。下划线在默认编辑态持续隐藏 HTML 标记并绘制真实下划线，支持跨行以及与粗体、行内代码组合，同时保留源码/显示字符映射。
- 链接按钮会在同一浮层内切换为 `Paste a link…` 输入框和 `Set` 操作；可为选区创建标准 `[文本](地址)`，裸域名补全 HTTPS，内部相对路径和锚点保持原样；再次编辑链接文字时读取既有地址，清空地址则解除链接。
- `Ask AI` 点击后会在格式条上方展开 340px、16px 圆角的输入面板，包含 Sparkles、需求输入和 40px 圆形发送按钮；目前按规划仅完成 UI、中文 IME/回车输入与空值禁用，不连接模型、网络或任何 AI 后端。
- 支持 `Cmd/Ctrl+A` 全选、`Cmd/Ctrl+C` 复制、`Cmd/Ctrl+X` 剪切和 `Cmd/Ctrl+V` 文本/图片粘贴；输入、删除、换行和粘贴会替换当前选区。
- 复制读取 Markdown 源码选区；文本粘贴通过 GPUI 系统剪贴板读取并统一 CRLF/CR 换行，不会把多行 Markdown 压成一行。
- 剪贴板图片粘贴会保留 PNG/JPEG/WebP/GIF/SVG/BMP/TIFF 原格式，写入当前笔记同级 `assets/` 目录并插入相对 Markdown 图片语法；时间戳文件名带碰撞递增保护，不覆盖既有附件。
- Markdown 呈现已从手写逐行字符规则迁移到 writ 的 tree-sitter-md `RenderSnapshot`、结构标记、行内样式和 buffer/display 映射。
- 当前可呈现 ATX/Setext H1–H6、无序/有序/任务列表、带高对比竖线的引用块、全宽主题分割线、带语法高亮的围栏代码块、粗体、斜体、下划线、删除线、行内代码、链接、独立/行内图片和基础 GFM 表格；源码仍是保存权威数据。编辑器显式使用 Inter，并内置 Google Fonts 官方 Normal/Italic 可变字体及独立的 700 Bold/Bold Italic 字体面；粗体由 GPUI 实际匹配独立粗体字形，不再只修改无法可靠生效的可变字重字段。行内代码使用平台等宽字体，并绘制 Ornata 同款主题 hover 背景、4px 圆角和约 0.15em/0.4em 的视觉内边距；围栏代码块不重复套用行内代码背景。
- writ 的重叠样式运行会转换为 GPUI 完整连续的 `TextRun`，链接下划线、强调、代码色和删除线可组合呈现。
- 默认编辑模式保持持续渲染的结构化笔记视图：光标进入标题、粗体、斜体、行内代码、链接、表格、任务、图片、公式或 Mermaid 时不会自动暴露 Markdown 源码；右上角源码按钮是查看和编辑完整原始 Markdown 的唯一入口。
- 编辑状态由会话持续跟踪，`Modified` 通过页签脏状态圆点反馈；底部状态文案随底部工具栏一并移除。
- 支持 `Cmd+S` / `Ctrl+S` 原子保存回本地 Markdown 文件。
- 脏文档可以安全切换到其他页签，各页签独立保留未保存缓冲区和光标。
- 切换 Vault 前会保护未保存内容；选择器取消不改变当前状态，打开失败会保留原 Vault 与文件树并显示错误。
- 编辑器使用 GPUI wrapped layout 按中心栏实际可用宽度软换行并动态增加行高，长中文、英文、URL 和连续标识符不再向右延伸或被裁切。
- 软换行只影响显示，不向 Markdown 源码插入换行；折行后的鼠标命中、光标、跨行选区和输入法范围共用同一二维布局坐标。
- 编辑器采用 `w-full + 1120px max-width` 响应式居中写作页，按扣除侧栏后的实际编辑区宽度使用 16/24/32px 水平边距和 24px 顶部留白，不再显示代码编辑器式行号。
- 宽屏编辑区右侧空白区域提供纯 Rust + GPUI 磁性短线笔记目录：按文档 revision 缓存 H1–H3 与真实行索引，默认仅用 6.08px 弱化刻度表示章节，当前章节只加深；悬停后按 0/1/2/3 距离扩展为 27.52/18.88/12.16/6.08px 并显示自适应标题卡片，点击通过原生变高 `ListState` 定位标题。目录以正文真实右边缘为基准并保留 16px 间距，右侧空白无法容纳 40px 刻度栏、至少 160px 标题卡片和安全间距时整体隐藏，绝不压入或遮挡正文；不依赖 DOM、HTML/CSS、JavaScript、WebView 或 Web Audio。
- 正文使用系统 UI 字体、16px 字号与 1.65 行高；标题按 1.6em、1.3em、1.1em 建立紧凑层级。
- 默认编辑态不再显示标题、强调、链接等 Markdown 语法标记；无序列表持续由 GPUI 独立绘制 5px faint 圆形 marker，有序序号同样使用 faint 层级。右上角源码模式逐字显示原始 Markdown。
- 分割线在 `w-full + 1120px max-width` 正文页内使用完整可用宽度，绘制 1px `line` 主题色，并以 65px 块高提供上下各 32px 间距。
- GFM 表格按连续块解析统一列数：默认编辑态持续显示等宽单元格，语法分隔行隐藏，表头使用侧栏/panel 表面色与半粗体，单元格使用 1px 主题边框和 6×10px 内边距；活动表格块继续接收原生输入，不自动恢复源码。
- 引用块使用连续、无圆角的 2px `ink` 左侧竖线，正文使用 muted 色；浅色主题竖线为深色、深色主题竖线为亮色，并保留 0.8em 块间距。
- Obsidian 风格提示块复用 Writ 的 CalloutInfo 与 13 类类型/别名识别：标题行隐藏 `[!TYPE]` 源码并显示类型图标和默认/自定义标题，标题与正文使用连续的类型色左边框和主题透明背景；普通引用块不受影响。
- 行内脚注引用 `[^label]` 显示为小号上标，代码片段中的同形文本不会误识别；脚注定义、命名标签和四空格/Tab 续写段落使用带顶部分隔线的独立小号区域，并保留定义内容中的常用行内 Markdown 呈现。
- H1–H4 按 Markd 的 1.6/1.3/1.1/1.0em、620/580/560/550 字重和对应上下间距呈现；H5/H6 回归 16px 正文字号与普通字重，所有标题继承同一个主题 `ink`，不再被 writ 额外叠成 700 粗体。
- 围栏代码的开闭标记在阅读状态隐藏，代码内容使用 panel 背景、line-soft 边框、8px 圆角、14×16px 内边距、0.86em 等宽字体；writ/tree-sitter 语言高亮色会进入 GPUI `TextRun`，不再被统一正文色覆盖。
- Mermaid 围栏代码块使用纯 Rust `rusty-mermaid 0.2.0` 解析和 SVG 后端，通过 GPUI 原生 Image 显示；图表在默认编辑态持续渲染，根据正文宽度等比缩放并匹配浅色/深色主题，完整围栏源码由右上角源码模式显示，语法错误显示可编辑错误卡片。
- Mermaid SVG 按 Vault、笔记路径、文档 revision 和主题缓存；普通光标移动复用已有图表，不重复解析或生成 SVG。
- 行内 `$...$`、单行 `$$...$$` 和多行 `$$` 数学公式块使用纯 Rust `RaTeX 0.1.14` 解析、排版并生成嵌入字形的自包含 SVG，通过 GPUI 原生 Image 显示，不依赖 WebView、JavaScript、网络或系统 LaTeX。
- 行内公式按文本基线对齐，块级公式在正文内容宽度内居中并等比缩小；光标进入公式所在行/块或点击预览仍保持渲染，完整 LaTeX 通过右上角源码模式编辑；转义美元符号、行内代码和普通货币文本不会被误渲染。
- 数学公式 SVG 按 Vault、笔记路径、文档 revision、主题和源码模式缓存；解析/排版错误会显示可编辑错误状态，不崩溃也不改写 Markdown。
- Markdown 图片不再折叠为 `🖼 alt` 占位字符：独立图片按固有尺寸在正文宽度和 720px 高度内居中显示，行内图片以 22px 高度参与文本流；光标进入图片行或点击独立预览仍保持渲染，完整图片语法由右上角源码模式显示。
- 本地图片相对当前笔记目录或 Vault 根目录解析，支持百分号编码中文/空格路径，并通过 canonicalize 阻止 `..` 与符号链接越过 Vault；HTTP(S) 图片使用启动时注入的真实 Rust HTTP 客户端，Reqwest/Hyper 请求固定运行在单工作线程 Tokio runtime 内，再把响应交回 GPUI 原生异步加载及全局解码缓存；支持 HTTPS、最多 10 次重定向和 30 秒超时，失败时显示 alt 与错误状态。
- 编辑器在页签下方新增 40px 笔记工具行：左侧显示文件夹到当前笔记的面包屑，右侧提供 Markdown 源码模式切换和笔记更多菜单；源码模式使用等宽原始 Markdown 与恒等光标映射。
- 编辑器右上角的源码切换与更多操作复用无边框 `text` 按钮：移除原有 28px 内层圆角、边框和背景，只显示 Lucide 图标；40×40px 命中区与 Tooltip 继续保留，源码激活状态仅通过图标前景色反馈。
- 笔记更多菜单已连接导出 Markdown、复制 Markdown 和删除笔记；导出使用系统保存面板，复制写入系统剪贴板，删除继续走未保存保护与系统废纸篓路径。
- 页签、文件夹、笔记及笔记操作菜单中的 Lucide 图标均设置固定尺寸、禁止收缩和显式主题色，避免 `currentColor` 未继承导致菜单只显示文本。
- macOS 使用与当前主题融合的透明标题栏；文档页签直接占据 44px 标题栏，侧栏显示时交通灯位于侧栏顶部留白，侧栏隐藏时页签栏预留 84px 系统按钮安全区。
- 文件树选择笔记会创建顶部文档页签；再次选择已打开笔记只切换现有页签，不会重复打开或重载。
- 多个页签分别保留 Markdown 缓冲区、Unicode 光标位置和 `Saved` / `Modified` 状态。
- 页签支持点击切换、右侧 `×` 关闭和脏状态圆点提示；固定宽度内的过长标题强制单行并以省略号收尾。
- 激活页签自身使用与编辑器一致的单一表面色，仅隐藏底部分割线；其他边缘保持普通直角页签样式，不绘制圆角、遮罩或装饰 Canvas，所有页签继续显示普通右侧分隔线。侧栏开关直接显示 Lucide `panel-left` / `panel-right`，无背景和圆角容器，同时保留 40px 点击区域与提示文本。
- 页签右键菜单支持 `Close`、`Close Left`、`Close Right`、`Close All`。
- 批量关闭采用原子保护：目标范围内存在未保存页签时全部拒绝，不会部分关闭或丢失内容。
- 左侧导航与中心编辑器是一体化连续工作区，仅使用 1px 分隔线。
- Lucide `panel-left` / `panel-right` 侧栏开关位于 44px 标题栏中、首个页签左侧，使用 40px 命中区；编辑器底部工具栏已完全移除。
- 左侧 Settings 底栏保持 40px 高度和顶部分隔线，不再与已删除的编辑器底栏建立布局耦合。
- 页签右键菜单改为监听原生 `MouseButton::Right` 按下事件，修复右键无响应。
- 页签右键菜单以鼠标点击位置为锚点，并按 8px 窗口边距自动限制位置，不再固定显示在编辑器右侧。
- 左侧导航顶部搜索入口使用 Lucide 放大镜、`Search any...` 默认文案和右侧 `⌘K` 徽标；搜索内容行使用与 248px 侧边栏、8px 外边距和 12px 输入框内边距对应的固定可用宽度，快捷键始终贴齐输入框右侧内边缘；点击会打开居中的 560px 命令面板和暗色遮罩。
- 命令面板包含 New Note、Open Vault、Todo、Bookmarks、Settings 入口；其中 Open Vault 继续使用原生文件夹选择器。
- 搜索入口下方包含 Todo 与 Bookmarks 快捷行，以及 Notes 标题、新建文件/文件夹图标和底部 Settings 入口。待办行对齐 Ornata 的 30px 集合行：左侧 Lucide `list-todo` 与名称只负责打开待办工作区，右侧固定 40px 独立命中区使用 Lucide `plus` / `minus`，只负责展开或收起下方快捷列表，两块区域不会串行动作。
- 待办快捷列表读取完整待办集合，不受当前标签或完成状态过滤；完成项仍保留在原顺序中，以勾选、faint 文字和删除线显示，点击可恢复为未完成。列表使用 12px 文本、12px 原生复选框、最大 144px 滚动区和 150ms 高度/透明度过渡；只有右侧展开图标能够改变其开关状态。
- 点击侧边栏“待办”主区域会切换到编辑器位置的独立 GPUI 待办工作区。待办工具栏显示“待办”面包屑和“新建标签”，主体使用 900px 居中内容页、168px 标签筛选栏和右侧待办区域。
- 标签栏提供“全部”、彩色标签圆点、选中状态和右对齐真实使用计数；右侧使用带 20px Lucide 加号的原生输入框创建待办，加号直接设置 SVG 主题色而不再依赖父级继承，浅色/深色主题均可见；输入支持中文 IME、回车提交、首尾空白清理、空值/长度校验、当前标签自动关联和新项目置顶。
- 待办列表不再绘制项目间分割线；每行提供 40px 复选框命中区，完成后即时显示勾选、弱化文本和删除线。悬停行最右侧显示分配标签、复制、删除三个 40px 操作，标签浮层打开期间保持可见。
- 分配标签浮层直接锚定图标下方并限制在窗口安全边距内，列出全部标签色圆点和勾选状态；点击不会关闭面板，可连续选择多个标签。已分配项在文本下方显示为 `#标签` 彩色胶囊，右侧 X 可单独取消关系。
- 复制操作直接写入待办纯文本到系统剪贴板；删除操作从当前列表和本地持久化文件中完全移除待办，并即时更新标签计数与筛选结果。
- 待办引导文案同行右侧会按全部已完成项目的实时数量显示“清除已完成 · N”；点击一次删除所有完成项、保留未完成项并立即持久化，数量归零后操作自动隐藏。
- “新建标签”会原位展开为 GPUI Component 原生无边框透明 Input，支持中文 IME；容器宽度以 180ms `EaseInOutCubic` 展开，输入框与按钮以 140ms `EaseOutQuad` 交叉淡化，输入框右侧是两个 24px 圆形 hover 图标按钮（勾选/取消）。确认会校验空名称、48 字符上限和不区分大小写的重名，成功后立即添加并选中新标签；Enter 提交、Esc 取消、X 取消均会清空并恢复文本按钮。
- 待办标签栏的选中态不再使用逐行静态背景：激活 pill 改为绝对定位的 6px 圆角浅色胶囊，以 Markd `SPRING_LAYOUT` 同源参数（`TagPillSpring` 360/32/0.6 经 180ms 归一化）在“全部”与各标签行之间平滑滑动，反向点击从当前位置继续；选中行文字切为 `ink`，计数随选中状态在 muted/faint 间切换。
- 标签名称按原顺序持久化到平台用户配置目录的 `Synapse/todo-tags`；待办 ID、文本、完成状态和多标签关系持久化到 `Synapse/todo-items`，重启后恢复。新版长度编码继续兼容旧单标签记录；没有引入 WebView、HTML/CSS、JavaScript 或新的运行时依赖。
- 文件区域会从 Vault 相对路径生成父目录优先的分层目录/笔记行，并按深度缩进；点击笔记继续复用多页签逻辑。
- 文件树内置并原生注册与 Markd 同源的 Inter 可变字体；文件名和文件夹名统一使用 13px 字号、30px 行高，普通文件为 400 字重、文件夹为 500 字重。默认名称使用 muted（浅色 `#6e6e6a`、深色 `#8f8f8a`），激活或悬停使用 ink（浅色 `#191919`、深色 `#ebebe8`）；折叠文件夹不再额外降低整行透明度。
- 左侧侧栏内容保持固定 248px 宽并始终挂载，独立裁切视口在 248px 与 0px 间执行 180ms 可中断宽度动画；编辑器 flex 空间与 macOS 标题栏安全边距同步变化，连续反向点击会从当前插值状态继续，不会闪回起点或留下空白栏。
- UI 图标已统一为 Lucide `1.27.0` 官方 SVG：覆盖搜索、待办、书签、新建文件/文件夹、目录、Markdown 文件、设置、页签关闭和侧栏开关。
- Lucide SVG 通过 GPUI 资源源编译进应用，不依赖启动目录或用户机器上的外部资源；许可证和固定版本来源随项目保存。
- 可在 Vault 根目录或任意子目录直接创建 `未命名N` 文件夹/Markdown 笔记；序号按目标目录中同级未命名文件与文件夹数量生成并执行碰撞保护。
- 新建笔记自动写入 `# 未命名N`、立即打开并把光标放在一级标题末尾，不再先打开新建命名弹层。
- 本次自动新建会话内，编辑首行一级标题会实时重命名对应 `.md` 文件；非法名称或重名时保留编辑缓冲区和原文件名并显示错误。
- 手动右键重命名会把当前文件树行直接切换为输入框；支持中文 IME、UTF-16/UTF-8 安全组合文本、Enter 提交、Escape 取消和原位错误反馈。
- 文件夹和笔记均有类型专属右键菜单，支持递归新建、重命名、Finder 定位和可恢复的系统废纸篓操作。
- 文件树行使用普通 pointer 光标；文件夹/笔记右键菜单按实际点击位置和真实面板尺寸限制在窗口 8px 内，可直接连续右键另一行切换目标，不再由全屏透明 backdrop 阻断目标命中。
- 页签、文件树与笔记操作右键菜单已移除菜单项分割线，保持连续一体化面板。
- 文件夹/笔记支持拖到目录、同级笔记所在目录或列表空白处完成真实移动。
- 文件操作会立即刷新侧栏快照；干净页签自动跟随新路径，脏页签会阻止影响其文件的重命名、移动或删除。
- 自动新建笔记保存时若发现原路径已不存在，会重新扫描 Vault；仅在一级标题对应唯一 Markdown 文件时安全更新页签路径并重试保存，同名歧义时保持缓冲区并报错。
- 固定接入 `gpui-animation 0.2.63`：目录状态、搜索面板、页签激活和所有当前右键菜单按动画规约执行 140～180ms 可中断过渡，浮层淡出后再卸载；侧栏为避免宽度残留改为即时布局切换。
- 独立顶部工具栏和编辑器底部工具栏已完全移除；标题栏保留侧栏开关、页签和可拖拽空区，Open Vault 保留在空状态和命令面板。
- 界面接入最新正式版 `gpui-component 0.5.1` 与 `gpui-component-assets 0.5.1`，窗口使用组件 Root 和可切换主题。
- 空状态、侧栏入口、新建控件、标题栏侧栏开关/页签关闭、两类右键菜单和命令面板操作已迁移到标准 Button；界面表面色、边框、状态色和文本层级优先读取主题 token。
- 命令面板搜索区域使用标准 Input，具备组件库原生输入法、选择、剪贴板、光标和焦点状态；全局 `Cmd/Ctrl+K` 会打开面板并聚焦搜索框。
- 搜索快捷键提示使用标准 Kbd，组件默认图标资源与项目固定 Lucide 资源通过同一个 GPUI AssetSource 提供。
- Settings 已改为 `WindowKind::Normal` 原生独立窗口，不再覆盖主窗口编辑器，也不再使用弹窗/遮罩；它拥有自己的系统标题栏、关闭、最小化、缩放和自由调整尺寸能力。重复点击 Settings 会激活已有窗口而不是叠加创建。
- Settings 窗口创建已退出主应用当前点击事件的实体更新栈后再执行，避免新窗口首帧同步读取仍处于可变更新中的 `SynapseApp` 导致 GPUI 重入 panic；快速重复点击由独立 opening 状态合并。
- Settings 及其他 macOS 原生窗口装饰现与全局 System / Light / Dark 偏好同步：Dark 使用 AppKit `Dark Aqua`，Light 使用 `Aqua`，System 清除应用级覆盖并恢复系统外观；原生标题栏不会再与深色内容区形成浅色断层。
- Settings 独立窗口继续使用 gpui-component 官方 `Settings`，Appearance 支持 System、Light、Dark；System 会监听系统外观变化，选择结果保存到用户配置目录。
- 浅色主题使用 `#fbfbfa` 编辑画布和 `#f4f4f2` 侧栏，深色使用 `#1a1a1a` 与 `#151515`，保证导航与写作区层级可辨。
- Markdown 呈现结果按 Vault、笔记路径、文档 revision、光标和主题缓存；光标闪烁、侧栏动画及无关状态刷新不再重复复制 Rope 和解析全文。
- 编辑器渲染缓存现在长期持有与当前文档 revision 对齐的 Writ Buffer；中文输入、组合输入、Backspace、Delete 和选区替换直接在该 Rope 上按字节范围增量更新，下一帧复用解析状态，不再为每个按键重新创建并全量解析 Writ Buffer。缓存同步通过 revision 校验，不复制全文，也不在调试构建中逐键全文比较。
- 自动新建笔记的一级标题/文件名联动（包括中文 IME 组合提交）只在编辑范围触及首行时执行；正文区域的连续输入与删除不再提取全文标题、触发文件系统重命名判断或重新扫描 Vault。文档目录也直接复用本帧已解析行生成，不再执行第二次 Markdown 解析。
- 编辑器使用 GPUI 原生变高虚拟列表，只创建、测量和塑形可视范围及上下 320px 缓冲区，长文档不再一次性构造全部行元素。
- 编辑行布局通过共享缓存直接回写，不再在每一行 paint 后调用应用实体更新；离开视口的命中布局会随滚动清理。
- 源码/显示字符映射使用一次性 Unicode 字节边界表；release 非视觉探针从旧算法约 79.7ms 降至约 46.4µs。
- 侧栏收起/展开只执行一次 0/248px 布局切换，不再逐帧插值宽度或延迟卸载；页签标题栏和编辑器在同一次布局中自动扩展或收缩。

## 正在开发

- Markdown 加粗已从“只写入 650 权重字段”修正为注册并匹配 Inter 700 Bold / Bold Italic 独立字体面；字体 OS/2 表回归会直接验证权重与 bold/italic 标志，避免再次出现字段正确但实际仍选中 Regular 的假修复。行内代码继续使用真实等宽字体与主题背景。按要求未启动应用、未执行截图或自动 UI 操作，等待产品手动验收。
- 斜体、连续输入/删除性能与光标可见性调整已通过全工作区 148 项测试（另有 1 项手动性能探针按设计忽略）、两个 Synapse 包定向 Fmt、`cargo check -p synapse`、全工作区 Clippy 严格检查和 `git diff --check`；新增回归锁定斜体样式运行、Unicode 增量 Writ 编辑、正文编辑不触发标题/路径联动、Rope 首行长度及目录复用。按要求未启动应用、未执行截图或自动 UI 操作，等待产品手动验收快速 Backspace/Delete 跟手性、斜体字形和两种主题下的光标视觉。
- 划词工具条内容可见性修复已通过全工作区 145 项测试（另有 1 项手动性能探针按设计忽略）、全工作区 Clippy 严格检查、定向 Fmt 和 `git diff --check`；按要求未启动应用或执行截图测试，等待产品手动验收深浅主题下的 `Ask AI`、格式图标、链接确认及禁用/可用发送状态。
- 编辑器文本划词功能已通过全工作区 145 项测试（另有 1 项手动性能探针按设计忽略）、`cargo fmt --manifest-path crates/synapse-ui/Cargo.toml -- --check`、`cargo check -p synapse`、全工作区 Clippy 严格检查和 `git diff --check`；新增 3 项定向回归锁定中文选区格式包裹/取消、粗斜体区分、链接上下文解析与地址规范化，以及下划线的中文、嵌套样式、编辑态和跨行原生呈现。按要求未启动应用或执行截图测试，等待产品手动验收真实鼠标拖选、软换行/跨行定位、链接输入和 Ask AI 展开效果。
- 编辑器工具栏纯图标调整已通过 `cargo fmt --manifest-path crates/synapse-ui/Cargo.toml -- --check`、`cargo check -p synapse`、全工作区 Clippy 严格检查和 `git diff --check`；`cargo fmt --all --check` 仅被仓库既有 vendored GPUI Component 与当前 rustfmt 的导入排序差异阻断，本轮未改动第三方目录。按要求未启动应用或执行截图测试，等待产品手动验收静止、源码激活及更多菜单三种状态。
- Vault 外部变化刷新已通过全工作区 142 项测试（另有 1 项手动性能探针按设计忽略）和 `cargo clippy --workspace --all-targets -- -D warnings`；新增回归锁定“发现外部新增条目但保持未保存文档缓冲区”。按要求未启动应用或执行截图测试，等待产品从 Finder/终端/下载器手动验收真实文件系统事件。
- 侧栏集合图标对齐调整已通过 `cargo check -p synapse`、侧栏字体/布局定向回归、Clippy 严格检查和 `git diff --check`；按要求未启动应用或执行截图测试，等待产品手动确认光学对齐效果。
- 标签栏本轮调整已完成代码与非视觉验证；`cargo test --workspace` 通过 141 项测试（另有 1 项手动性能探针按设计忽略），`cargo clippy -p synapse --all-targets -- -D warnings` 通过。按要求未启动应用、未执行截图或自动 UI 操作，等待产品手动验收两类工作区的标签 hover、滑动背景和字体视觉。
- 完整书签功能已完成代码与非视觉验证；`cargo test --workspace` 通过 140 项测试（另有 1 项手动性能探针按设计忽略），`cargo clippy -p synapse --all-targets -- -D warnings` 和 `git diff --check` 均通过。按产品要求未启动应用、未执行截图或自动 UI 操作，等待产品手动验收真实站点元数据、系统浏览器跳转、保存面板和浅色/深色主题视觉效果。
- 编辑器滚动、标题栏页签/侧栏开关、侧栏联动、列表标记和响应式正文宽度整改已完成，等待产品使用真实长文档与多种窗口尺寸手动验收；按要求不启动应用、不执行截图或自动 UI 操作。
- P2 基础原生输入、鼠标/键盘范围选择、系统剪贴板、列表回车语义、光标闪烁和 writ 标准解析呈现已实现，等待产品手动验收实际鼠标拖选、系统剪贴板和样式效果。
- 当前首要缺口已转为撤销/重做、按词移动/选择、双击选词、三击选行，以及 HTML、定义列表等专用组件渲染。
- 已完成 `writ 0.18.1` 技术评估：其解析、源码/显示映射、选择、撤销、表格和 Markdown 呈现能力高度匹配需求，但最新版已从 GPUI 迁移到 winit + Vello + Parley，不能作为现有 GPUI 界面的直接子组件；是否采用需先做隔离原型。
- 自动截图和自动界面操作已按要求停止；后续根据产品手动验收继续修正交互问题。

## 尚未实现

- 最近 Vault 记录和多 Vault 管理界面。
- 页签拖拽排序、跨窗口移动、重启后的工作区恢复和未保存关闭确认弹窗。
- 搜索输入、结果过滤与键盘导航；Settings 已完成主题与工作区设置，后续仍需补充编辑器、文件管理等其他偏好分类。
- 完整专业编辑器能力中的按词移动/选择、双击选词、三击选行、撤销/重做和查找替换。
- HTML、定义列表的专用组件渲染；当前未专门渲染的扩展语法仍安全显示源码。
- 搜索业务逻辑和文件监听。
- 自动保存、最近打开记录、其余设置和专注模式。
- 发布级启动速度、内存和大文件性能认证。

## 质量状态

| 检查项 | 状态 | 最近结果 |
|---|---|---|
| 阶段 0 规格校验 | 通过 | 98/100；无 HTTP API 警告属于桌面应用预期 |
| V1 规格校验 | 通过 | 98/100；唯一提示为本地桌面应用无 HTTP API |
| V2 规格校验 | 通过 | 98/100；唯一提示为本地桌面应用无 HTTP API |
| V3 文件管理回归 | 通过 | 核心、会话、资源与平台命令测试全部通过 |
| Rust 格式化 | 通过 | Synapse 两个 workspace 包分别通过 `cargo fmt --package … -- --check`；vendor 保持上游源码原样 |
| Clippy | 通过 | workspace/all-targets，warnings 视为错误 |
| 自动化测试 | 通过 | 165 个 Synapse workspace 测试全部通过，另有 1 个手动 release 性能探针按需运行；供应组件上游 102 个测试此前已通过 |
| GPUI 原生交互 | 部分通过 | IME、鼠标定位/拖选、Shift 选区、全选/复制/剪切/粘贴、列表续写、可点击任务框、光标闪烁、提示块、脚注、Mermaid 与数学 SVG 已接入；其余扩展语法组件和高级编辑行为仍需补齐；按要求未运行截图或自动界面操作 |
| 编辑器与侧栏性能 | 待手动验收 | 已完成解析缓存、GPUI 变高虚拟列表、共享布局回写、线性字符映射和非布局侧栏动画；字符边界 release 探针约 79.7ms → 46.4µs。按要求未启动应用，尚无真实帧率数据 |

## 开发日志

### 2026-08-10 — 初始化与 Vault 只读发现

- 建立 `synapse-core` 与 `synapse-ui` Cargo workspace。
- 锁定 GPUI `0.2.2`，开发环境启用 `runtime_shaders`。
- 实现 Vault 校验、Markdown 递归发现、稳定排序及错误模型。
- 实现原生三区域应用壳并完成 smoke check。
- 新增阶段 0 规格与 12 个自动化测试。

### 2026-08-10 — 启动笔记编辑纵向切片

- 用户确认继续推进实质功能，并要求建立持续同步的进度文档。
- 本轮范围确定为“选择笔记 → 编辑内容 → 保存回本地文件”。
- 新增 `specs/note-editing.md`，定义读取、Rope 编辑、路径安全、原子保存与 UI 交互契约。
- 新规格严格校验结果为 98/100；唯一警告仍为本地桌面应用无 HTTP API。
- 新增 11 个核心验收测试，覆盖读取、Unicode 编辑、原子保存与路径安全。
- TDD RED 已确认：新 API 尚未实现，测试按预期编译失败。
- 引入稳定版 `ropey 1.6.1`，实现 `NoteDocument`、Unicode 字符索引编辑、脏状态和修订号。
- 实现 Vault 内安全打开和同目录临时文件原子保存；拒绝路径逃逸、非 Markdown 与符号链接。
- 修正规范路径断言后，核心 20 个测试全部通过。
- 补充未保存内容保护规格：脏文档必须保存后才能切换，重复选择当前笔记不会重载。
- 新增 UI 会话状态与 8 个编辑交互测试；UI crate 当前共 11 个测试全部通过。
- 文件树已接入点击选择，中心编辑区已接入 Rope 内容、光标、键盘操作与保存快捷键。
- `cargo check --workspace` 已通过。
- 全量格式化、Clippy 与 31 个 workspace 测试已通过。
- 增加 Unicode 光标显示与尾随空行的渲染测试。
- 按产品方要求停止自动截图/界面操作测试，并清理本轮临时测试 Vault。
- 更新 README，补充真实编辑能力、快捷键和当前限制。
- 最终非视觉回归通过：格式化、Clippy、33 个测试全部成功。
- 笔记编辑规格最终校验 98/100；唯一警告为桌面应用无 HTTP API。
- 产品方手动验收发现关键缺口：普通启动无 Vault 入口，界面不可操作。
- 诊断确认当前运行进程未传 Vault 参数，程序落入空状态；代码只实现了命令行路径入口。
- 更正此前错误结论：底层编辑与保存逻辑已实现并测试，但用户可达的完整产品流程尚未完成。
- 当前状态：纵向切片重新打开，优先补齐应用内 Vault 打开能力。

### 2026-08-10 — 补齐应用内 Vault 打开入口

- 新增空状态主按钮和侧边栏 `Open` 按钮，无参数启动后可直接操作。
- 接入 GPUI 系统原生文件夹选择器，仅允许单选文件夹。
- 选中目录后执行 Vault 校验与 Markdown 扫描，并立即替换文件树和状态。
- 选择器取消时保持原状态；选择器或 Vault 打开失败时显示具体错误。
- 打开新 Vault 采用先校验后替换，失败不会清空已打开 Vault 的笔记列表。
- 活动笔记存在未保存内容时拒绝切换 Vault，避免内存编辑内容丢失。
- 新增 3 个会话状态回归测试，覆盖成功打开、失败保留和脏文档保护。
- UI crate 16 个测试全部通过，且未启动应用、未执行截图或自动 UI 测试。
- 最终非视觉回归通过：格式化、Clippy（warnings 视为错误）、36 个 workspace 测试及开发版构建全部成功。

### 2026-08-10 — 修复长行布局与标题栏主题

- 定位中心编辑栏缺少最小宽度约束的问题：长文本的内容宽度会参与父级 flex 计算并把右侧栏推出窗口。
- 中心栏、编辑区、行容器和文本节点统一增加可收缩约束，并隐藏中心面板边界外溢出。
- 编辑文本启用软换行，长行保持在编辑器可视宽度内；行号保持固定宽度。
- 编辑器标题增加截断保护，状态文本保持固定，避免长文件名破坏标题栏布局。
- 窗口最小尺寸设置为 900 × 560，保证三栏在允许的窗口尺寸内均有可用空间。
- macOS 标题栏改为透明全尺寸内容模式，顶部深色背景与正文统一，并为交通灯按钮保留 40px 区域；其他平台保留系统标题栏。
- 新增 macOS 标题栏配置测试；UI crate 17 个测试通过。
- 按要求未启动应用、未执行截图或自动 UI 操作。
- 最终非视觉回归通过：格式化、Clippy（warnings 视为错误）、37 个 workspace 测试及开发版构建全部成功。

### 2026-08-10 — 实现 V1 多页签与一体化工作区

- 完整读取 `docs/Task.md` V1 规划，并新增 `specs/v1-workspace-tabs.md` 作为实现契约。
- V1 规格严格校验 98/100；唯一提示为本地桌面应用无 HTTP API，属于预期。
- 测试先行确认 RED：多页签状态 API 尚不存在时，25 处编译错误按预期出现。
- 将单活动文档会话升级为 `OpenTab` 集合，每个页签独立保存 `NoteDocument` 和 Unicode 字符光标。
- 文件树选中新笔记会追加页签；选择已打开笔记会复用原页签并保留未保存缓冲区与光标。
- 新增页签激活、单项关闭、关闭左侧、关闭右侧、关闭全部和非法索引错误模型。
- 活动页签关闭后优先激活原右侧邻居；右侧不存在时回退到左侧邻居。
- 所有关闭操作检查目标范围脏状态；批量操作发现任一脏页签会整体拒绝。
- Vault 切换现在检查所有打开页签，而非只检查一个活动文档。
- 中心区新增可横向滚动的页签栏，包含截断标题、选中态、脏状态圆点及关闭按钮。
- 页签右键菜单已接入 `Close`、`Close Left`、`Close Right`、`Close All`，点击外部区域可关闭菜单。
- 原浮动三卡片布局已替换为连续工作区，Explorer/Editor/Backlinks 之间仅保留 1px 分隔线。
- 顶部 40px 统一工具栏与 macOS 交通灯水平对齐，包含 Vault 信息、状态、打开 Vault 及左右侧栏开关。
- 左右侧栏可独立收起和展开，释放宽度由编辑器自动占用，不影响页签和文档状态。
- 更新旧笔记编辑规格，明确 V1 多页签行为取代“脏文档禁止切换笔记”。
- 新增 7 个 V1 状态回归测试；最终格式化、Clippy、44 个 workspace 测试和开发版构建全部通过。
- 按要求未启动应用、未执行截图或自动 UI 操作。

### 2026-08-10 — 实现 V2 Markdown 编辑器导航

- 完整读取 `docs/Task.md` V2 规划并检查两张参考图的层级、尺寸和视觉密度。
- 新增 `specs/v2-editor-navigation.md`，明确 V2 UI 范围以及搜索、Todo、Bookmarks、Settings、文件创建逻辑的后续边界。
- V2 规格严格校验 98/100；唯一提示为本地桌面应用无 HTTP API，属于预期。
- 测试先行确认 RED：右键触发与分层文件树纯函数尚不存在时测试按预期编译失败。
- 页签新增原生 `MouseButton::Right` mouse-down 监听；右键释放不会再被普通点击逻辑立即覆盖。
- 页签关闭图标区分左右键，右键不会误触发关闭。
- 移除默认 Backlinks 右栏和对应顶部开关，界面收敛为 Markdown 编辑器主区与左侧文件导航。
- 左侧导航按参考图重构：搜索入口、MY NOTES、Todo、Bookmarks、分隔线、Notes 工具栏、分层文件树和底部 Settings。
- Notes 工具栏新增独立的新建文件与新建文件夹图标入口，当前保持非破坏性 UI 占位。
- 新增 560px 中央命令面板、暗色遮罩、外部点击关闭及 New Note/Open Vault/Todo/Bookmarks/Settings 命令行。
- Open Vault 命令保持真实可用；其他 V2 明确延后的入口只改变 UI 状态，不触碰文件系统。
- 新增 `FileTreeRow` 与父目录去重算法，基于现有发现快照生成分层目录和笔记行，不重新扫描或读取文件内容。
- 重写 README 与产品需求文档，将产品定位从本地知识工作台调整为高性能、本地优先 Markdown 编辑器。
- 当前产品路线移除双向链接、反向链接和知识图谱，历史规格增加 V2 supersede 说明。
- 新增 2 个 V2 回归测试；最终格式化、Clippy、46 个 workspace 测试和开发版构建全部通过。
- 按要求未启动应用、未执行截图或自动 UI 操作。

### 2026-08-10 — 准备 Lucide 图标资源目录

- 新建 `assets/icons/lucide/`，用于存放项目实际使用的 Lucide SVG 图标和许可证。
- 添加 `.gitkeep`，确保图标尚未导入时目录仍可被版本控制保留。

### 2026-08-10 — 接入官方 Lucide 图标系统

- 从 Lucide 官方仓库固定获取 `1.27.0` 版本，导入项目实际使用的 12 个 SVG 和完整许可证。
- 新增统一的 `Icon` 枚举和 `SynapseAssets` GPUI 资源源；所有 SVG 使用 `include_bytes!` 编译进应用，发布后不依赖相对文件路径。
- 替换 UI 内搜索、Todo、Bookmarks、加号、新建文件、新建文件夹、目录、Markdown 文件、Settings、页签关闭和侧栏开关的 Unicode 伪图标。
- 图标统一采用 Lucide 24 × 24 视口、2px 圆角描边，并按界面密度使用 14–17px 显示尺寸与一致的暗色层级。
- 侧栏开关会根据当前展开状态切换 `panel-left-close` / `panel-left-open`，不再使用含义模糊的字符。
- README 已同步图标技术选型、固定版本和编译期内置方式。
- 新增资源完整性测试，逐一验证 12 个声明图标已内置且保留 `currentColor` 描边能力。
- 已用 XML 规范化对比确认 vendored SVG 与官方对应图形语义一致；官方 LICENSE 内容逐字一致。
- 非视觉回归全部通过：格式化、Clippy（warnings 视为错误）、47 个 workspace 测试、开发版构建。
- 按产品要求未启动应用、未运行截图或自动界面操作。

### 2026-08-10 — 完成 V3 文件夹与笔记管理

- 完整读取 `docs/Task.md` V3 与 `docs/文件夹笔记功能.md`，新增 `specs/v3-folder-notes.md` 作为实现与安全契约。
- 固定引入跨平台系统废纸篓库 `trash 5.2.6`；文件夹删除和笔记移除均进入系统废纸篓，不执行永久删除。
- 核心层新增包含空目录的完整 Vault 条目快照，并支持根目录/任意子目录的新建文件夹、新建 Markdown 笔记、重命名、移动、绝对路径解析和废纸篓操作。
- 文件操作拒绝路径穿越、非法多组件名称、非 Markdown 扩展名、符号链接、目标覆盖和文件夹移入自身或子孙目录。
- 会话层每次成功操作立即刷新侧栏快照；干净页签跟随重命名/移动后的新路径，脏页签会阻止影响其文件的操作。
- Notes 标题按钮与命令面板 New Note 已从占位入口升级为真实命名弹层，支持 Enter 提交、Escape/Cancel 关闭和内联错误反馈。
- 文件夹右键菜单实现 New Folder、Reveal in Finder、New Note、Rename、Delete Folder；笔记右键菜单实现 Rename、Reveal in Finder、Move to Trash。
- 所有 V3 菜单项采用 Lucide 图标和文字；危险操作使用红色图标、文字与悬停状态。
- 文件夹图标按需求使用 Lucide `folder`，新增 `folder-search`、`pencil`、`trash-2`，当前 16 个 SVG 全部编译进应用。
- GPUI 原生拖拽支持移入文件夹、移到笔记所在目录和移回 Vault 根目录；核心层负责碰撞及递归移动保护。
- README 与产品需求文档已同步 V3 当前能力、依赖、安全边界和后续阶段。
- 新增 12 个 V3 测试；最终格式化、Clippy（warnings 视为错误）、59 个 workspace 测试和开发版构建全部通过。
- 按产品要求未启动应用、未运行截图或自动界面操作。

### 2026-08-10 — 完成 V3 文件树与即时新建交互调整

- 扩展 V3 规格 FR-15～FR-20，明确目录展开图标、空目录占位、同级未命名序号、自动一级标题和标题驱动文件重命名。
- 文件树新增内存折叠状态；点击目录切换展开/收起，展开使用 Lucide `folder-open`，收起使用 `folder` 并隐藏所有后代。
- 展开的无子项目录会生成独立浅色“空文件夹”占位行，收起后占位同步消失。
- Notes 工具栏、目录右键菜单和命令面板的新建入口移除预命名步骤，直接创建同目录 `未命名N`；文件与文件夹共用同级序号计数并保持覆盖保护。
- 核心层创建 Markdown 笔记时写入与文件名一致的一级标题；自动新建后立即打开笔记并把光标定位到标题末尾。
- 自动新建的活动笔记建立一级标题联动：标题编辑成功时同步重命名磁盘文件和页签路径；非法或碰撞时保留脏缓冲区与最后一个有效文件名并显示错误。
- 手动右键重命名继续使用现有弹层，并会解除该笔记的自动标题联动，防止后续正文编辑覆盖用户的显式文件名选择。
- 新增 5 个回归测试并更新创建内容断言；格式化、Clippy（warnings 视为错误）、64 个 workspace 测试和开发版构建全部通过。
- 按产品要求未启动应用、未运行截图或自动 UI 操作。

### 2026-08-10 — 修复移动后保存仍访问旧路径

- 根据报错路径只读核对实际 Vault：嵌套笔记创建于 21:39:14，21:39:16 被移动到 Vault 根目录；目录与根文件的元数据变化时间一致，确认磁盘文件已移动但活动页签仍引用旧路径。
- 保存流程新增受限恢复：首次保存遇到 `NotFound` 时，仅针对本次自动新建且保持标题联动的笔记重新扫描 Vault。
- 只有首行一级标题在全 Vault 对应唯一 Markdown 文件时，才重定位活动文档、刷新文件树快照并重试原子保存。
- 找不到候选或存在多个同名候选时不会猜测、创建或覆盖文件，继续保留内存缓冲区并显示原始错误。
- 新增 2 个回归测试，分别覆盖嵌套笔记被移到根目录后的成功恢复，以及同名歧义时拒绝写入错误文件。
- 最终格式化、Clippy（workspace/all-targets 且 warnings 视为错误）、66 个 workspace 测试和开发版构建全部通过。
- 按产品要求未启动应用、未运行截图或自动 UI 操作。

### 2026-08-10 — 完成 P1 原位重命名、底栏和交互动画

- 新增 `specs/p1-inline-rename-and-motion.md`，锁定原位重命名、中文 IME、底栏布局、页签菜单位置和动画验收边界。
- 删除重命名模态弹层；文件夹与笔记点击 Rename 后在原文件树行渲染可编辑输入框，失败时保留输入内容并显示红色输入边框。
- 新增独立 GPUI `EntityInputHandler`，处理 marked/committed 文本、UTF-16 与 UTF-8 范围换算、中文输入法组合态、选择、删除、方向键、Home/End、Enter 和 Escape。
- 页签右键菜单状态新增鼠标坐标；菜单显示位置从固定 `right: 16px` 改为点击锚点，并在窗口四周保留 8px 安全边距。
- 移除顶部左侧栏开关，将官方 Lucide `panel-left` / `panel-right` 固定到编辑器 40px 底栏最左侧；左侧 Settings 行同步调整为 40px。
- 替换并清理旧 `panel-left-open` / `panel-left-close` 资源，当前 16 个实际使用的 Lucide SVG 继续编译进应用。
- 固定新增 `gpui-animation 0.2.63`，为目录状态、命令面板、页签激活、左侧栏宽度和所有右键菜单接入 140～180ms 过渡；浮层关闭使用带代次保护的延迟卸载，避免旧计时任务关闭新浮层。
- 左侧栏收起到 0 宽度时同步裁剪溢出内容，避免不可见导航继续覆盖编辑器或拦截鼠标。
- 将 `docs/过渡和交互动画规约.md` 完善为可执行标准，补充统一时长、缓动、稳定 ID、退出卸载、验收要求及后续开发约束。
- 新增 3 个回归测试，覆盖中文 UTF-16/UTF-8 边界、页签菜单点击定位/窗口限制和底栏等高约束。
- 最终格式化、Clippy（workspace/all-targets 且 warnings 视为错误）、69 个 workspace 测试和开发版构建全部通过。
- 按产品要求未启动应用、未运行截图或自动 UI 操作。

### 2026-08-10 — 初始化并推送 GitHub 远程仓库

- 在项目根目录初始化 Git，默认分支设为 `main`，远程 `origin` 配置为 `git@github.com:08820048/synapase.git`。
- 推送前确认 `target/`、`.DS_Store` 和交换文件均由 `.gitignore` 排除，未发现待提交密钥、凭据或私钥文件。
- 创建首次提交 `541e472`（`feat: build Synapse V3 editor interactions`），包含当前核心、UI、测试、规格、Lucide 资源和进度文档。
- 首次提交已成功推送到远程 `main`，本地分支已设置为跟踪 `origin/main`。

### 2026-08-10 — 完成 P2 原生编辑器输入与 Markdown 实时呈现

- 新增 `specs/p2-editor-input-and-preview.md`，明确顶部标题、搜索入口、鼠标光标、上下移动、中文 IME、Markdown 源码/呈现映射及非视觉验证边界。
- 移除工作区工具栏左侧 `Synapse / <Vault>` 标题组，状态和 Open Vault 操作保持在右侧。
- 侧栏搜索入口按参考图收敛为左侧 Lucide 放大镜、`Search any...` 文案和右侧 `⌘K` 徽标；命令面板提示同步更新。
- 删除依赖 `KeyDownEvent.key_char` 的临时英文字符输入路径，编辑器正式实现 GPUI `EntityInputHandler`，覆盖 UTF-16 选区、中文/emoji 范围转换、marked composition 更新、提交和候选窗定位范围。
- 输入法组合期间延迟自动新建笔记的一级标题文件名联动，提交后再同步重命名，避免候选文本导致文件被反复重命名。
- 新增真实 GPUI shaped-line 编辑行元素，保存每行文本布局和边界；鼠标点击按实际字形 x 坐标映射到最近的源码 Unicode 字符位置。
- 新增 Up / Down 操作和快捷键；跨长短行时保存期望字符列，Left / Right / Home / End 或鼠标点击后重置期望列。
- 新增可编辑的 Markdown 实时呈现层：标题隐藏源码前缀并按层级显示，列表/引用转换为视觉符号，代码与常用成对行内标记隐藏源码定界符；保存内容仍为原始 Markdown。
- 建立源码字符、呈现字符、UTF-8 字节、UTF-16 单元四种索引的显式映射，保证预览隐藏标记后鼠标和输入法仍落在正确源码位置。
- 新增 3 个编辑状态与 3 个呈现映射测试，并移除 2 个旧假光标字符串测试；最终测试总数为 73 个。
- 按产品要求未启动应用、未运行截图或自动 UI 操作；仅执行代码、状态、格式化、Clippy、测试和构建验证。

### 2026-08-11 — P2 Markdown 编辑器产品验收未通过

- 产品实际体验确认当前编辑器仍不具备标准 Markdown 编辑器的完整行为，P2 从“等待验收”重新打开。
- 当前技术实现明确为 Rust + GPUI `0.2.2` 原生界面、Ropey 文本缓冲区和项目内手写的轻量 Markdown 行呈现器；尚未接入 pulldown-cmark、markdown-rs、tree-sitter 或成熟编辑器内核。
- 记录首批阻断项：无序列表/有序列表回车后不自动续写，空列表项不能通过回车退出；编辑光标为固定绘制竖线，没有周期性闪烁；多数 Markdown 块级和行内语法缺少解析与呈现。
- 后续不能再将当前轻量呈现描述为完整 Markdown 编辑器，需要以编辑命令层、标准解析层、光标/选择状态机和可扩展渲染层为基础继续建设。

### 2026-08-11 — 评估 writ Markdown 编辑器库

- 核对 crates.io 当前最新版 `writ 0.18.1`、发布包源码、功能开关和编辑命令实现；本轮仅做只读技术评估，未修改依赖或产品代码。
- `writ` 提供 Ropey 缓冲区、tree-sitter-md 增量解析、源码/显示位置映射、选择、剪贴板、撤销重做、表格、任务列表、折叠、代码高亮、图片、Mermaid 和数学公式等成熟能力，明显优于当前手写轻量呈现层。
- 当前最新版的桌面与渲染栈是 winit + wgpu/Vello + Parley，不再是 GPUI；`MarkdownView` 输出 Vello Scene，`core::Editor` 是无头编辑模型，因此都不能直接作为 GPUI `IntoElement` 嵌入。
- docs.rs 当前公开的 GPUI `Editor` 页面对应旧版 `0.12.0`，而 crates.io 最新版为 `0.18.1`；不建议为直接嵌入长期锁死旧版并放弃后续修复。
- `writ 0.18.1` 的普通 Enter 设计为原始换行，Shift+Enter 才续写列表/引用；与产品要求的普通 Enter 自动续写相反，需要在集成层重新映射命令。
- `writ 0.18.1` 当前 shell 每次绘制都固定绘制 caret，未发现周期性显隐状态；采用该库仍需自行补充光标闪烁。
- 建议下一步建立隔离原型，只评估 `writ::core::Editor` 与解析/位置映射模块能否复用，同时继续由 GPUI 承担应用壳和最终绘制；原型通过前不替换现有编辑器。

### 2026-08-11 — 接入 writ 第一阶段编辑命令与光标闪烁

- 新增 `specs/p2-writ-editor-adapter.md`，锁定 GPUI 应用壳不变、关闭 writ 默认 app feature、Unicode 索引边界、列表 Enter 语义和光标闪烁验收标准。
- workspace 固定引入 `writ 0.18.1` 且 `default-features = false`；依赖树确认未启用 writ 的 winit、Git、GitHub、网络、文件监听或 GhostText 功能。
- 新增 `markdown_command` 适配层，使用公开的 `writ::editor::EditorState`、tree-sitter-md 行标记和 continuation 数据生成最小字符范围编辑，不在 Enter 时整篇替换 Synapse Rope。
- 普通 Enter 现在自动续写 `-`/`*`/`+` 无序列表、递增 `1.`/`1)` 有序列表、未勾选任务列表和引用容器；空列表项回车移除标记并退出列表。
- 保留 Shift+Enter 作为不续写容器的原始换行，避免用户需要在列表正文内主动断开结构时失去控制。
- 新增独立 `CursorBlinkState`：约 530ms 切换可见状态，使用代次令牌让输入、点击、方向移动和 IME 更新立即显示光标并中止旧计时循环。
- 当时采用混合编辑呈现：活动光标所在行恢复完整 Markdown 源码标记，离开后再隐藏标记；该行为已被 2026-08-13 的“默认富文本渲染态与显式源码态对齐 Ornata”整体替代。
- 光标只在编辑器拥有焦点时实际绘制；计时任务通过弱实体更新，应用实体释放后自然终止。
- 新增 4 个 writ 命令测试、1 个会话保存测试、2 个光标闪烁状态测试和当时的活动行源码测试；其中活动行源码测试现已改为验证默认富文本态在光标移动时保持稳定，其余命令、持久化与闪烁回归继续有效。
- 本阶段没有启动应用、没有运行截图或自动 UI 操作；格式化、Clippy（workspace/all-targets 且 warnings 视为错误）、81 个 workspace 测试和开发构建全部通过。

### 2026-08-11 — 诊断编辑器无法粘贴

- 核对编辑器 action 与快捷键注册，确认当前只有保存、删除、方向移动和换行命令，没有 `Paste` action，也没有 `Cmd+V` / `Ctrl+V` 绑定。
- 当前 `EntityInputHandler` 负责输入法组合文本与普通文本替换，但应用级剪贴板快捷键不会自动进入 `replace_text_in_range`，因此粘贴按键被忽略。
- GPUI `0.2.2` 已提供 `App::read_from_clipboard()` 和 `ClipboardItem::text()`，故问题属于 Synapse 编辑器集成遗漏，不是系统剪贴板不可用。
- 本轮只完成代码级根因诊断与进度同步，没有修改产品功能，也没有启动应用或运行截图/自动 UI 测试。

### 2026-08-11 — 新增 Markdown 语法完整性测试文档

- 在 `docs/Markdown语法完整性测试.md` 新增可直接通过 Synapse 打开的综合 Markdown 测试资料。
- 覆盖 CommonMark 与常见 GFM 语法：六级标题、段落、软/硬换行、强调、转义、嵌套列表、任务列表、引用、链接、图片、代码、表格和分隔线。
- 覆盖常见扩展语法：Front Matter、提示块、脚注、定义列表、数学公式、Mermaid、HTML、折叠块及上下标。
- 增加中文、emoji、组合字符、超长行、连续空行和源码解析边界案例，并附带输入、粘贴、光标、列表续写和保存的手动验收区。
- 本轮只新增测试资料与进度记录，没有修改应用功能，也没有启动应用或运行截图/自动 UI 测试。

### 2026-08-11 — 完成基础选区、剪贴板和 writ 标准解析呈现

- 新增锚点/活动端组成的 `EditorSelection`，选区使用 Unicode 字符索引并向 GPUI 输入协议报告正确的 UTF-16 范围与方向。
- 编辑器区域新增鼠标按下、移动、释放状态机，支持跨行拖选和 Shift 延伸；选区按源码范围映射到各显示行并在文字下方绘制统一背景。
- 新增 Shift + 方向键/Home/End、`Cmd/Ctrl+A/C/X/V` action 与快捷键；Backspace、Delete、普通输入、IME 提交、换行和粘贴均可替换选区。
- 复制/剪切读取原始 Markdown 源码，粘贴调用 GPUI 系统剪贴板并保留多行内容；Windows/macOS 风格 CRLF/CR 会规范为 `\n`。
- 移除原先逐行识别少量字符的手写预览器，改用 writ `RenderSnapshot`、tree-sitter-md 块标记、行内样式和 `SegmentMap` 生成 GPUI 显示行。
- 新增 ATX/Setext 标题、无序/有序/任务列表、引用、分隔线、围栏代码、粗体、斜体、删除线、行内代码、链接、图片占位和 GFM 表格行呈现；表格转为视觉分隔符并保留转义竖线。
- 将 writ 可重叠样式覆盖层切分为完整、连续、互不重叠的 GPUI `TextRun`，并保留 Unicode 源码字符与显示字符的双向映射。
- 新增覆盖选区方向/拖动、剪贴板换行、链接/任务/图片、表格/代码/分隔线、Setext 标题、转义表格竖线和 383 行综合 Markdown 测试资料一致性的回归测试。
- `make-interfaces-feel-better` 技能促使本轮把选区反馈绘制在文字下层、保持 I-Beam 光标与交互状态连续，并避免新增与编辑无关的装饰动画。
- 按产品要求没有启动应用、没有运行截图或自动 UI 操作；格式化、Clippy（workspace/all-targets 且 warnings 视为错误）、88 个 workspace 测试和开发构建全部通过。

### 2026-08-11 — 修复 Markdown 编辑区软换行回归

- 根因确认：自绘 `MarkdownLineElement` 使用只能处理单行的 `shape_line`，且请求高度固定为一个 line-height；外层 `width: 100%` 只能限制布局宽度，无法让已塑形文字自动折行。
- 将编辑行改为测量式布局，调用 GPUI `shape_text` 并传入父容器的 definite width；元素高度根据 wrap boundary 数量动态增长。
- `EditorLineLayout` 从单行 `ShapedLine` 升级为 `WrappedLine`，鼠标命中从单一 x 坐标改为二维 position，折行后的点击可定位到对应源码字符。
- 光标位置、输入法范围和选区绘制全部改用 wrapped layout；跨多个视觉行的选区被拆成连续矩形，软换行不修改或保存为源码换行。
- 编辑器行号固定对齐到源码行的第一视觉行，不再因长段落增高而垂直居中。
- `make-interfaces-feel-better` 的排版规范用于约束实现：长段落使用正常流式换行，不对长文本使用平衡换行，也没有增加与修复无关的动画。
- 新增视觉行 byte range 连续性测试；按产品要求没有启动应用、没有运行截图或自动 UI 操作；格式化、Clippy（workspace/all-targets 且 warnings 视为错误）、89 个 workspace 测试和开发构建全部通过。

### 2026-08-11 — 提交并推送 P2 编辑器功能

- 将 P2 原生输入、选区与剪贴板、writ Markdown 解析呈现、综合语法测试文档和软换行修复统一提交为 `ed1efd4`（`feat: upgrade Markdown editing experience`）。
- 功能提交已成功推送至 `origin/main`；远程仓库为 `git@github.com:08820048/synapase.git`。
- 推送前确认本地与远程没有分叉，并检查本批文件未包含私钥或常见明文凭据。

### 2026-08-11 — 使用 gpui-component 重构应用界面

- 新增 `specs/gpui-component-ui-refactor.md`，明确组件版本、Root/Theme/Button/Input/Kbd 迁移范围、领域专用控件保留边界与非视觉验收标准。
- 接入最新正式版 GPUI `0.2.2`、`gpui-component 0.5.1` 和 `gpui-component-assets 0.5.1`；尝试官方主分支后确认其使用尚未在 Rust `1.93.1` 稳定的 `std::hint::cold_path`，因此没有把项目切到不可稳定构建的开发分支。
- 为同时保留最新版 writ `0.18.1`，本地供应上游 `gpui-component 0.5.1`，仅将其 tree-sitter 清单要求从 `0.25` 对齐到 `0.26`，组件源码行为未修改。
- 应用启动先初始化组件系统并应用当前外观偏好；窗口第一层改用 Root，为标准弹层、通知和输入焦点提供统一宿主。
- 主要按钮与右键菜单操作迁移为 Button；常规、主操作、幽灵和危险状态统一使用组件变体，移除重复的手写 hover/active 样式。
- 命令面板搜索区迁移为 Input，侧栏及面板快捷键提示使用 Kbd；新增全局 `Cmd/Ctrl+K` Action，打开面板时自动聚焦组件输入框。
- Synapse 自有 Lucide AssetSource 增加组件默认资源回退，既保留现有固定图标，也支持组件内置 IconName。
- `make-interfaces-feel-better` 规范用于本轮按钮命中面积、嵌套圆角、主题表面层级、图标光学对齐和交互状态收敛；未增加与迁移无关的动画。
- 依赖图确认只有 GPUI `0.2.2` 和 tree-sitter `0.26.12` 各一份；供应组件的 102 个上游测试及 Synapse 当前 93 个 workspace 测试全部通过。
- 按产品要求没有启动应用、没有运行截图或自动 UI 操作；Synapse 两个 workspace 包的格式化、Clippy（workspace/all-targets 且 warnings 视为错误）和开发构建全部通过；vendor 保持上游原始格式，不纳入本机 rustfmt 重写。

### 2026-08-11 — 编辑器排版与三态主题

- 新增 `specs/editor-typography-and-theme.md`，记录 Markd 参考参数、主题行为、持久化要求和非视觉验收边界。
- 编辑区改为占可用区域 92%、最大 1100px 的响应式居中写作列，水平内边距收窄至 24px，保留 24px 顶部留白和充足底部滚动空间；删除全部可见行号。
- 修正首版机械照搬 Markd `720px + 40px` 参数导致 Synapse 默认窗口正文过窄的问题；现在仅保留适度居中间隙，不再出现左右大片空白。
- 正文调整为系统 UI 字体、16px/26.4px，标题按 1.6em、1.3em、1.1em 比例和约 1.25 行高呈现。
- 扩展 writ 到 GPUI 的样式运行映射：光标行暴露的块级和行内 Markdown 标记使用 muted token，正文、光标和选区颜色均跟随当前主题。
- 建立参考 Markd 的单色浅/深色调色板，分别使用不同的画布和侧栏表面色，并映射到 gpui-component 的背景、侧栏、页签、菜单、输入和选择 token。
- Settings 入口现在打开真实 Appearance 面板，可选 System、Light、Dark；System 通过 GPUI appearance observer 实时跟随系统，偏好保存到用户配置目录并在启动时恢复。
- `make-interfaces-feel-better` 规范用于控制正文行长、系统字体层级、弱化标记对比、表面分层、40px 最小命中区域与克制的现有浮层交互，没有增加无关动画。
- 新增编辑器排版常量、三态主题解析/表面区分和 Markdown 标记弱化测试；按产品要求未启动应用或运行截图测试。

### 2026-08-11 — 编辑器滚动与侧栏动画性能静态审计

- 确认性能问题来自当前渲染架构，没有发挥 GPUI 的增量/保留式优势，不能归因于 Rust 或 GPUI 本身。
- `SynapseApp::render` 每次构建编辑区都会调用 `NoteDocument::text()`，把整棵 Rope 分配为新的 String，随后 `source_lines` 新建 writ Buffer、重新解析整篇 Markdown 并构造所有行的显示映射。
- 编辑器只使用普通 `overflow_y_scroll`，内部通过 `children` 一次性创建全部源码行；每一行都调用 `shape_text` 完成换行和字形整形，没有按视口虚拟化。
- 每个编辑行在 paint 阶段调用一次应用实体更新，将布局写回 `editor_line_layouts`；该数组还会在每次 render 时按全文行数重新分配。
- 光标闪烁约每 530ms 调用一次 `cx.notify()`；由于解析与行构建位于顶层 render，单纯改变光标可见性也会连带触发全文工作。
- 源码/显示字符映射循环为每个字符反复从字符串开头执行 `char_indices().nth()` 和前缀 `chars().count()`，长段落存在接近平方级的 CPU 放大。
- 左侧栏过渡逐帧把宽度从 0 改到 248px；编辑器可用宽度随每帧变化，导致全部 Markdown 行重新换行、整形和布局，是展开/收起“粘滞”的直接原因。
- 本轮仅完成代码级静态诊断并同步进度，没有修改产品功能、没有启动应用、没有截图或运行自动 UI 操作；因此不虚构帧率或耗时数据。

### 2026-08-11 — 完成编辑器滚动与侧栏动画核心性能整改

- 新增 `EditorRenderCache`，以 Vault 根目录、笔记相对路径、`NoteDocument::revision`、光标和深浅主题作为精确失效键；缓存命中时直接复用 `Rc` 行呈现，不再调用 `NoteDocument::text()` 或 `source_lines`。
- 普通 `overflow_y_scroll + children(all lines)` 已替换为 GPUI 原生变高 `ListState`，仅渲染视口和上下 320px overdraw，继续支持软换行产生的动态行高。
- 内容变化通过首尾差异计算只 `splice` 受影响的行区间，保留虚拟列表其余行的测量结果和滚动位置；新增回归测试验证局部失效范围。
- `SourceLine` 在缓存、虚拟列表元素与命中布局之间使用 `Rc` 共享；行 paint 直接写入共享布局槽位，彻底移除原有逐行 `Entity::update`。
- 虚拟列表滚动处理器清理视口外命中布局，避免不可见旧行参与鼠标坐标判断。
- 源码/显示映射改为预计算 Unicode 字节边界并通过 `partition_point` 查找，消除逐字符反复 `char_indices().nth()` 与前缀 `chars().count()` 的平方级路径。
- release 非视觉性能探针使用 1 万个混合中文/ASCII/emoji 字符：旧重复扫描约 `79.728ms`，新边界表约 `46.416µs`，该局部热路径约缩短 1700 倍。
- 侧栏宽度从动画属性移出：关闭时先在固定 248px 宽度完成 180ms 透明度过渡，再一次性释放布局宽度；打开时一次性预留宽度后淡入，代次令牌防止快速连续点击触发旧卸载。
- 新增 `ShellState::vault_root()`，避免切换到具有相同相对路径和 revision 的另一个 Vault 时错误复用旧 Markdown 缓存。
- 非视觉验证全部通过：格式化、workspace/all-targets 严格 Clippy、94 个自动化测试、开发构建和 `git diff --check`；另单独通过 release 性能探针。
- 按产品要求没有启动应用、没有截图、没有运行自动 UI 操作；因此滚动帧率和侧栏体感仍明确留给产品手动验收，不以纯函数基准冒充整机帧率。
- 本批 gpui-component 界面重构、主题/排版和性能整改已提交为 `ef7bc53`（`feat: modernize and optimize GPUI editor`），并成功推送至 `origin/main`。

### 2026-08-11 — 标题栏页签、侧栏联动与响应式正文宽度重构

- 直接浅克隆并阅读 Markd 当前主分支源码，确认其主区采用“侧栏 + 独立主工作区”，页签位于 44px Tauri overlay 标题栏；侧栏隐藏后主工作区占满窗口，正文采用 `w-full + max-width` 而不是永久百分比缩进。
- 删除原 40px 全局顶部工具栏及其右上角状态/Open Vault 操作；文档页签上移为主工作区第一行的 44px 自定义标题栏，空白区域注册为原生窗口拖拽区，页签本身继续可点击、关闭和右键。
- macOS 侧栏顶部新增 44px 交通灯/拖拽安全区；侧栏隐藏后标题栏左侧自动切换为 84px 交通灯安全区，侧栏显示时主标题栏只保留 10px 起始间距。
- 页签关闭 Button 命中区由 28px 扩大到 40px，符合界面规范的最小交互面积，同时保留 14px Lucide 图标的视觉尺寸。
- 侧栏收起逻辑移除 180ms 延迟卸载和 mounted/generation 双状态，点击后直接在同一次布局中把宽度切换为 0/248px；页签栏和编辑器同步向左扩展，不再残留空白列。
- 保存状态从被删除的顶部工具栏迁移到底栏右侧；Open Vault 仍可通过无 Vault 空状态和命令面板触发，不丢失功能入口。
- 正文容器移除 `width: 92%`，改为 `width: 100%; max-width: 1120px`；按扣除可见侧栏后的主工作区宽度使用三档边距：小于 720px 为 16px、720～1119px 为 24px、1120px 及以上为 32px。
- 该宽度策略保留 Markd 的响应式原理，但没有机械复制其 `720px max-width + 40px padding`，避免 Synapse 在普通桌面窗口重新出现过窄正文和大块无效空白。
- `make-interfaces-feel-better` 规范用于约束 40px 命中区、仅在空白区域注册窗口拖拽、取消布局宽度动画以及窗口尺寸变化下的响应式边距。
- 更新 README、编辑器排版规格、P2 顶部区域规格和 gpui-component 验收描述，使文档与当前标题栏/宽度结构同步。
- 按产品要求没有启动应用、没有截图或自动 UI 操作；格式化、workspace/all-targets 严格 Clippy、94 个自动化测试、开发构建和 `git diff --check` 全部通过。

### 2026-08-11 — 标题栏控制、页签截断、侧栏图标与列表标记修复

- Todo、Bookmarks 及其行尾加号 Lucide SVG 增加固定尺寸、禁止收缩和显式 `muted_foreground`，修复图标依赖未继承 `currentColor` 而在浅色/深色主题中不可见的问题。
- 页签标题从简写 `truncate` 改为与 gpui-component 页签一致的 `overflow_hidden + text_ellipsis + whitespace_nowrap` 组合，长标题在固定页签宽度内稳定显示省略号，不挤压脏状态圆点和关闭按钮。
- `panel-left` / `panel-right` 侧栏开关移入 44px 标题栏并放在首个笔记页签左侧，保持 40px 命中区和主题弱化色；侧栏收起时仍随标题栏主区自动左移。
- 删除完整编辑器底部工具栏及 `LOCAL • MARKDOWN`、保存状态文案和底栏侧栏开关；侧栏 Settings 底栏保留独立 40px 高度。
- Markdown 行呈现会在编辑和预览两种状态中识别无序/有序列表标记范围并统一映射到主题弱化前景色；无序列表预览使用标准排版圆点 `•`。
- 对照 Markd 当前样式中的 `.prose-note li::marker { color: var(--faint) }` 落实列表标记层级，同时新增回归测试锁定预览标记弱化和标准圆点。
- 按 `make-interfaces-feel-better` 的最小命中区与光学对齐规范保留 40px 标题栏按钮命中面，图标视觉尺寸保持 17px，避免扩大图标本体造成标题栏失衡。
- 同步 README、P1 标题栏规格、P2 编辑器规格、排版主题规格与 gpui-component 验收文档，移除全部已过期的“常驻编辑器底栏”当前约束。
- 按产品要求没有启动应用、没有截图或自动 UI 操作；`cargo fmt -p synapse -- --check`、workspace/all-targets 严格 Clippy、95 个自动化测试、开发构建和 `git diff --check` 全部通过，1 个手动性能探针按设计忽略。

### 2026-08-11 — 无序列表圆点尺寸回调

- 撤销上一轮把标准 `•` 替换为大号黑圆 `●` 的过度修正，预览重新使用系统字体的标准排版圆点 `•`。
- 保留无序/有序列表标记在编辑态与预览态统一使用主题弱化色的修复，不回退颜色层级。
- 删除不再需要的圆点替换函数和 `●` 分支，writ 原始呈现结果直接进入源码/显示映射，代码路径更简单。
- 同步 README、列表回归测试和本进度文档；按要求未启动应用、未运行截图或自动 UI 操作。
- `cargo test -p synapse --bin synapse` 通过 30 项测试，1 个手动性能探针按设计忽略；严格 Clippy、格式化和开发构建通过。

### 2026-08-11 — Markd 与 Synapse 列表标记实现对比

- 核对 Markd 当前主分支：普通无序列表使用浏览器原生 `ul { list-style: disc; }`，有序列表使用 `ol { list-style: decimal; }`，并通过 `li::marker { color: var(--faint); }` 只调整原生 marker 的颜色。
- Markd 没有手绘圆点，也没有向正文插入 `•`；圆点的几何尺寸、基线和列表悬挂缩进由浏览器排版引擎结合当前字体与 `1.5em` 列表缩进计算。
- 对比时确认旧实现由 writ 将源码 `- ` 折叠替换为普通文本 `• `，再由 GPUI 与正文使用同一字号塑形；`list_marker_range` 只识别该字形并把对应 TextRun 改为 `muted_foreground`。
- 因此旧实现的圆点本质是“正文中的 Unicode 字形”，不是独立列表 marker；尺寸完全取决于系统字体的 U+2022 字形，不能直接等价于 Markd 的 CSS `disc`。
- 同时确认旧实现颜色未完全照搬，且 writ 对 `*` 和 `+` 生成的 `◦`、`‣` 没有被完整识别；这些缺口已在下一批“无序列表独立 GPUI marker 绘制”中修复。
- 本轮只完成源码对比并同步进度，没有修改软件功能、没有启动应用或执行截图测试。

### 2026-08-11 — 无序列表独立 GPUI marker 绘制

- `MarkdownStyleRun` 新增列表标记与隐藏圆点槽位状态，将列表 marker 从普通语法弱化区间中独立出来。
- writ 继续负责 Markdown 源码到显示位置的稳定映射，但其 `•`、`◦`、`‣` 字形改为透明 TextRun，仅保留字符宽度、鼠标命中、光标和选区映射。
- `MarkdownLineElement` 根据真实 shaped-line 中 marker 字形的起止位置，单独绘制直径 5px、圆角半径 2.5px 的 GPUI 圆形，并向上光学修正 0.5px；不再依赖不同系统字体的 U+2022 字形大小。
- 列表 marker 改用调色板 `faint` 层级，浅色为 `#a3a39e`、深色为 `#64645f`，与 Markd 的 `li::marker { color: var(--faint) }` 保持一致；其他 Markdown 源码符号仍使用 `muted_foreground`。
- `-`、`*`、`+` 三种无序列表源码现在全部由同一个圆形 marker 接管，修复 writ 输出 `◦`、`‣` 时未进入弱化范围的问题；有序列表序号使用同一 faint 色但保留文本形态。
- 独立 marker 在编辑态和预览态持续显示，符合 writ 结构标记始终 WYSIWYG 呈现的实际行为；隐藏占位保证保存的 Markdown 源码和现有映射不变。
- 新增测试覆盖独立 marker run、5px 尺寸、三种无序源码形式、编辑态持续接管和有序列表 faint 色槽位；同步 README、P2 编辑器规格、主题排版规格和本进度文档。
- 按产品要求没有启动应用、没有截图或自动 UI 操作；格式化、workspace/all-targets 严格 Clippy、96 个自动化测试、开发构建和 `git diff --check` 全部通过，1 个手动性能探针按设计忽略。

### 2026-08-11 — 全宽主题分割线与基础 GFM 表格渲染

- 对照 Markd 当前 `styles.css`：分割线采用 `border-top: 1px solid var(--line)` 与上下 `2em` 间距，表格采用全宽折叠边框、`6px 10px` 单元格内边距、`0.95em` 字号及 panel 表头背景。
- Markdown 分割线不再以固定数量的横线字符作为可见结果；编辑器在与正文完全相同的响应式页宽和水平 gutter 内绘制 1px `theme.border`，65px 块高提供上下各 32px 的稳定留白。
- 连续 GFM 表格行新增结构化元数据：按块统一列数、补齐缺失单元格、识别表头和语法分隔行，并保留转义管道符所在单元格的原始内容。
- 当时仅非活动表格行使用真实 GPUI 单元格结构；该限制已被 2026-08-13 的持续渲染编辑态替代，表格现在无论光标位置都保持等宽列、1px 主题边框、`6px 10px` 内边距和 panel 表头。
- 当时光标进入表格内容行会恢复该行 Markdown 源码；该交互已被 2026-08-13 的持续渲染编辑态替代，结构化表格现在通过覆盖 canvas 保持命中和输入接管，完整源码统一由右上角按钮显示。
- 新增回归测试覆盖表头/分隔行/正文元数据、列数、转义管道符和 Markd 对齐的分割线/表格尺寸常量；同步 README、P2 编辑器规格、主题排版规格和本进度文档。
- 按产品要求没有启动应用、没有截图或自动 UI 操作；格式化、workspace/all-targets 严格 Clippy、97 个自动化测试、开发构建和 `git diff --check` 全部通过，1 个手动性能探针按设计忽略。

### 2026-08-11 — P3 引用块、标题、代码块与笔记顶部操作

- 只读核对 Markd 当前主分支 `styles.css`、`AppShell.tsx`、`NoteBreadcrumb.tsx`、`CodeBlock.tsx` 和源码编辑器实现，确认引用块为 2px `ink` 直角竖线、标题统一继承 `ink`、代码块使用 panel/line-soft 表面，笔记工具行位于页签下方。
- 引用行新增连续块边界元数据；GPUI 为连续行绘制无圆角 2px `theme.foreground` 左边线，引用文字使用 muted 色，块首尾提供 0.8em 外间距。
- 修复标题“看起来过浓”的真实原因：H1–H6 原本虽然使用正文 `ink`，但 writ heading bold run 会把外层 semibold 再覆盖成 700；现在标题运行不再重复加粗，并按 Markd 的 H1–H4 精确字号/字重/间距及 H5/H6 正文继承规则呈现。
- 围栏代码新增 opening/closing/content 边界元数据：非活动围栏行折叠为 0 高度，内容行组合成 panel 背景、line-soft 边框和 8px 圆角的连续块，使用 0.86em 等宽字体和 14×16px 内边距。
- `MarkdownStyleRun` 新增稳定 RGBA 语法色槽位；writ/tree-sitter 已计算的关键字、类型、字符串、数字、注释等高亮色进入 GPUI `TextRun`，不再在适配层丢弃；inline code 同时修复为真正使用主题等宽字体。
- 新增 Markdown 源码模式：原始行使用等宽 13px 字体、恒等 Unicode 源码/显示映射，不执行标题、引用、代码、表格折叠；切换状态加入编辑器解析缓存键，避免错误复用阅读模式行。
- 页签下方新增 40px 笔记工具行；面包屑从 Vault 相对路径生成，文件夹 muted、ChevronRight faint、当前无扩展名标题 semibold `ink`，长路径分段截断。
- 右侧源码/富文本和更多按钮均保留 40px 命中区、28px 可见表面和 15px Lucide 图标；更多菜单的导出 Markdown、复制 Markdown、删除笔记分别接入系统保存面板、系统剪贴板和现有废纸篓/未保存保护。
- 页签右键菜单补齐 Close、Close Left、Close Right、Close All 图标；文件夹、笔记和笔记操作菜单的 SVG 全部设置 `flex_none` 与显式主题色，修复 `currentColor` 未继承时只显示文本的问题。
- Lucide 资源新增 ChevronRight、Code2、Pilcrow、MoreVertical、Download、Copy、CircleX，并继续通过应用 AssetSource 编译内嵌和统一完整性测试。
- 新增回归测试覆盖标题不重复加粗、连续引用边界、代码围栏边界、monospace/语法色、源码模式恒等映射、中文面包屑与全部图标资源；同步 README、主题排版规格、新增 P3 规格和本进度文档。
- 按产品要求没有启动应用、没有截图或自动 UI 操作；格式化、workspace/all-targets 严格 Clippy、100 个自动化测试、开发构建和 `git diff --check` 全部通过，1 个手动性能探针按设计忽略。

### 2026-08-11 — P3 菜单图标左侧布局修复

- 定位到 `gpui-component` `Button` 的内容顺序：`.label(...)` 会先于后添加的 child 渲染，导致原先按“图标 child + label”编写的菜单实际显示为“名称 + 右侧图标”。
- 新增统一 `menu_item_content` 行布局，不再混用 `Button::label`：先放图标节点，再放文本节点，从结构上固定为左图标、右名称。
- 图标使用 18px 固定槽位、15px Lucide 图形和 8px 间距；文本占据剩余宽度并左对齐，使同一菜单的图标列与名称起始位置稳定对齐。
- 已覆盖页签右键菜单 4 项、文件夹右键菜单 5 项、笔记右键菜单 3 项、笔记更多菜单 3 项，以及侧栏 Settings 入口，共 16 个带图标菜单入口；普通图标继续使用 muted 色，危险操作继续使用 danger 色。
- 新增菜单图标槽尺寸回归测试，同步 README、P3 规格与本进度文档；按要求不启动应用、不运行截图或自动 UI 操作。
- 按产品要求没有启动应用、没有截图或自动 UI 操作；格式化、workspace/all-targets 严格 Clippy、101 个自动化测试、开发构建和 `git diff --check` 全部通过，1 个手动性能探针按设计忽略。

### 2026-08-11 — 浅色主题表面色与危险菜单一致性调整

- 核对主题映射后确认侧栏 `#f4f4f2` 与编辑器 `#fbfbfa` 已存在；未激活页签此前错误复用了侧栏 panel 色，因此没有呈现需求指定的独立层级。
- 为页签新增语义化 `tab_inactive` 色槽：浅色未激活页签固定为 `#e9e9e6`，激活页签继续使用编辑器背景 `#fbfbfa`；深色未激活页签随后按补充要求调整为 `#0f0f0f`，激活页签使用编辑器背景 `#1a1a1a`。
- 定位到 Delete Folder、Move to Trash、Delete Note 的边框来自显式 `.danger().outline()`；三个入口现已统一改为无边框 ghost 菜单行，并仅让图标与文字使用 danger 色。
- 扩展浅色主题回归断言，锁定侧栏、编辑器、未激活页签三个精确色值和深色页签原行为；同步 README、主题规格和本进度文档。
- 按产品要求没有启动应用、没有截图或自动 UI 操作；格式化、workspace/all-targets 严格 Clippy、101 个自动化测试、开发构建和 `git diff --check` 全部通过，1 个手动性能探针按设计忽略。

### 2026-08-11 — 深色主题页签表面层级调整

- 按补充要求确认深色侧栏 `#151515` 与编辑器 `#1a1a1a` 已正确映射；需要调整的是未激活页签，此前仍为 `#151515`。
- 深色 `tab_inactive` 已改为 `#0f0f0f`；选中后继续切换为编辑器背景 `#1a1a1a`，形成“未激活页签最深、侧栏居中、编辑器与激活页签一致”的三层表面关系。
- 扩展主题回归断言，分别锁定深色侧栏、编辑器和未激活页签的精确色值；同步 README、主题规格和本进度文档。
- 按产品要求没有启动应用、没有截图或自动 UI 操作；格式化、workspace/all-targets 严格 Clippy、101 个自动化测试、开发构建和 `git diff --check` 全部通过，1 个手动性能探针按设计忽略。

### 2026-08-11 — P4 Mermaid 原生渲染与文件树右键交互

- 核对 Markd 当前主分支后确认其没有 Mermaid 实现；本轮仅参考 `FileTree.tsx` 的原生右键坐标和 `ContextMenu.tsx` 的窗口边缘限制逻辑，没有虚构或复制不存在的 Markd Mermaid 方案。
- 固定接入纯 Rust `rusty-mermaid 0.2.0` 及 SVG 后端：识别 `mermaid` 反引号/波浪线围栏，解析为 Scene，再生成 GPUI 原生 SVG Image，不引入 WebView、浏览器或 JavaScript 运行时。
- Mermaid 块只在开围栏行建立单一预览锚点，其余源码行在默认编辑态折叠；原先光标进入块内会恢复围栏源码，该交互已被 2026-08-13 的持续渲染编辑态替代，完整源码统一由右上角按钮显示，磁盘 Markdown 不被改写。
- 浅色和深色 Mermaid 分别使用 Synapse `#fbfbfa` / `#1a1a1a` 编辑器背景及配套节点、线条、文字色；图表在正文实际内容宽度和 520px 最大高度内等比缩放，不放大低分辨率图表。
- Mermaid SVG 以 Vault、相对路径、文档 revision 和主题为失效条件独立缓存；普通光标移动只重建活动行呈现，复用已有图表。解析错误显示带原因的可点击错误卡片，不会导致编辑器崩溃或阻止源码修正。
- 文件树目录和笔记行从 GPUI `ClosedHand` 对应的 `cursor_move` 改为普通 pointer；拖拽能力保留，但未开始拖拽时不再显示抓手。
- 文件夹/笔记右键继续以实际鼠标位置为锚点，并按 218×198px / 218×122px 真实面板尺寸限制在窗口 8px 安全边距内；删除整窗透明 context backdrop，改为根层 capture 关闭旧菜单后让当前目标继续接收同一次右键，从而支持连续切换目标。
- 移除页签、文件夹、笔记和笔记更多菜单中的全部项目分割线，菜单保持一个连续表面；命令面板自身的内容分组线不属于右键菜单，予以保留。
- 新增 `specs/p4-mermaid-and-context-menu.md`，同步 README 和根目录进度文档；没有覆盖产品方正在编辑的 `docs/文件夹笔记功能.md`。
- 新增 Mermaid 围栏/Unicode 范围、主题 SVG、缓存失效和文件树菜单尺寸/边缘限制回归测试。最终非视觉验证通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、105 个自动化测试、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按产品要求没有启动应用、没有截图、没有运行自动 UI 操作；实际图表视觉和连续右键手感留给产品手动验收，不用静态测试冒充视觉验收。

### 2026-08-11 — 修复文件树右键菜单纵向偏移

- 根据产品截图重新诊断：右键事件提供的是窗口坐标，但文件树和页签菜单把该坐标交给普通 flex 根节点下的 `.absolute().top()/left()`，父布局原点再次参与计算，因此上方条目的菜单会被整体推到窗口下方；此前只校验数值边缘限制，没有覆盖真实元素坐标空间，结论不完整。
- 对照当前依赖中的 GPUI `Anchored` 与 gpui-component 输入框右键菜单实现，文件树和页签菜单都改为 `deferred(anchored())`，明确使用 `Corner::TopLeft`、原始窗口点击坐标和 8px 窗口边距。
- 删除手写 `context_menu_position`、硬编码菜单高度和对应的伪定位测试；现在由 GPUI 在布局完成后测量菜单真实尺寸并直接换算到窗口坐标，不受侧栏滚动位置、flex 静态位置或父元素原点影响。
- 菜单宽度、动画、左图标布局、危险操作颜色、连续右键切换与无分割线效果保持不变；同一修复同时覆盖文件夹、笔记和顶部页签右键菜单。
- 同步 README、P4 规格和本进度文档。最终非视觉验证通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、103 个自动化测试、开发构建及本轮文件 `git diff --check` 全部成功，1 个手动性能探针按设计忽略；产品方并行编辑的 `docs/文件夹笔记功能.md` 第 60 行已有尾随空格，本轮未越权修改。
- 按产品要求没有启动应用、没有截图、没有运行自动 UI 操作；菜单实际视觉位置由产品重新打开当前构建后手动验收。

### 2026-08-11 — 修复 Mermaid 编辑态源码块割裂

- 根据产品截图确认割裂不是 Mermaid 语法问题，而是编辑态仍套用普通围栏代码的逐行显隐和边界规则：当前开围栏单独获得顶部容器，第一条图表语句又因 `is_first_content` 再次获得顶部圆角与外间距，形成上下两个块。
- Mermaid 光标位于块内任意位置时，现在强制恢复开围栏和闭围栏的原始文本、等宽弱化样式及源码/显示恒等映射；不再只显示光标所在的单个围栏行。
- 新增 Mermaid 专用连续边界规则：开围栏是整个容器唯一的顶部，闭围栏是唯一的底部，中间所有图表语句只保留连续左右边框；普通代码块原有行为不受影响。
- 新增回归测试覆盖“光标位于图表内容时开闭围栏同时可见、索引映射正确”和“开围栏/内容/闭围栏分别产生 top/none/bottom 边界”；同步 README、P4 规格和本进度文档。
- 最终非视觉验证通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、105 个自动化测试、开发构建及本轮文件差异检查全部成功，1 个手动性能探针按设计忽略；按要求没有启动应用、没有截图或自动 UI 操作。

### 2026-08-11 — P5 默认窗口与数学公式渲染

- 将无参数启动窗口从 1180 × 760 调整为 1809 × 1332 并保持居中，原有 900 × 560 最小尺寸约束不变；操作系统仍可按实际屏幕工作区限制窗口。
- 固定接入纯 Rust `RaTeX 0.1.14` 的 parser、layout、SVG 和类型模块，把 LaTeX 直接生成为嵌入字形路径的自包含 SVG，再交给 GPUI 原生 Image 绘制，不引入 WebView、JavaScript、网络请求或系统 LaTeX。
- 新增行内 `$...$`、单行 `$$...$$` 与多行 `$$` 公式块识别；跳过转义美元符号、行内代码和普通货币文本，并保留源码字符范围到阅读态显示范围的映射。
- 行内公式按文本基线嵌入，公式块在正文可用宽度内居中并等比缩小；原先活动公式会恢复源码，该交互已被 2026-08-13 的持续渲染编辑态替代，完整 LaTeX 源码统一由右上角按钮显示。
- 数学 SVG 与 Mermaid 一样按 Vault、笔记路径、文档 revision、主题和源码模式失效，普通光标移动复用预览；解析/排版异常被隔离为可编辑错误状态，不会导致进程崩溃或改写笔记。
- 新增 `specs/p5-window-and-math.md`，同步 README 和根目录进度文档，并核实现有 Markdown 语法完整性测试文档已包含行内及块级公式用例；没有覆盖产品方正在编辑的 `docs/文件夹笔记功能.md`。
- 新增默认窗口、数学语法元数据、SVG 生成/错误隔离和预览缓存回归测试。最终非视觉验证通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、110 个自动化测试、开发构建和本轮文件差异检查全部成功，1 个手动性能探针按设计忽略。
- 按产品要求没有启动应用、没有截图、没有运行自动 UI 操作；公式实际字形、基线和复杂 LaTeX 视觉效果留给产品手动验收。
- P4 与 P5 功能已通过提交 `8c81f03` 推送到 `origin/main`；本进度记录随后以独立文档提交同步，确保远程代码、规格和交付状态一致。

### 2026-08-11 — 脚注、提示块与任务列表原生渲染

- 确认 Writ 已提供任务 Checkbox marker 和 Obsidian CalloutInfo，但 Synapse 原有 GPUI 绘制层仅把任务文本弱化、把提示块当普通引用，脚注也没有任何结构标注；本轮补齐从解析结构到原生组件的转换，而不是继续用源码字符模拟 UI。
- 任务项新增复选框源码范围、正文起点和缩进元数据；阅读状态裁掉 `- [ ]` / `- [x]` 标记并保留正文已有行内样式，绘制 16px 圆角复选框和 Lucide check，点击后切换标准 Markdown 标记并保持原光标。
- 提示块把 Writ 的 13 类 CalloutKind、别名、默认/自定义标题和块范围映射到 GPUI；标题与正文形成连续的类型色左边框和低透明背景，普通引用继续使用原有高对比无圆角竖线。
- 脚注新增行内引用、定义和缩进续写段落检测；引用替换为小号上标，定义区使用顶部分隔线、固定标签列和 14px 正文，定义正文会独立经过 Writ 行内解析，粗体等标记不以源码形式泄漏。
- 源码模式不生成任何任务、提示块或脚注预览元数据；光标进入相关行时仍走原始编辑呈现，磁盘内容始终保持标准 Markdown。
- 新增 `specs/markdown-footnotes-callouts-tasks.md` 并同步 README 与根目录进度文档；现有 `docs/Markdown语法完整性测试.md` 已覆盖嵌套任务、NOTE/WARNING 提示块、数字/命名脚注和续写段落，无需制造重复样例。
- 新增 3 个回归测试覆盖任务预览与 Unicode 映射、提示块标题/连续边界、脚注引用误识别/定义/续写和行内样式。最终非视觉验证通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、113 个自动化测试、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按产品要求没有启动应用、没有截图、没有运行自动 UI 操作；实际复选框点击手感、提示块配色和脚注排版由产品手动验收。

### 2026-08-11 — Markdown 本地与远程图片原生渲染

- 按要求先将脚注、提示块和任务列表功能以提交 `10da559` 推送到 `origin/main`，随后开始本轮图片修复；远程同时报告默认分支仍有 1 个中等级别 Dependabot 提示，本轮不越权改动无关依赖。
- 根据产品截图定位到直接原因：`presentation_from_writ` 主动把 Writ 已识别的独立图片替换成 `🖼 alt` 文本，GPUI 层没有接收图片 URL、没有路径解析，也没有任何真实图片元素。
- 新增标准 Markdown 图片扫描与源码/显示范围元数据，覆盖独立图片、行内图片、尖括号目标、标题、转义、百分号编码和行内代码排除；活动行强制恢复完整图片源码，避免同一行其他图片被 Writ 折叠成不可编辑空位。
- 新增 Vault 安全图片解析：相对路径基于笔记目录，前导 `/` 基于 Vault 根目录，编码中文和空格会解码；canonicalize 后拒绝越过 Vault、缺失目标、目录目标以及 `file:`/`data:` 等非 HTTP(S) scheme。
- 独立图片使用 GPUI 原生 `img` 异步加载并按固有尺寸居中，在正文宽度和 720px 最大高度内约束；行内图片固定 22px 高并限制为 180px 宽，HTTP(S) 继续使用同一个原生加载器和全局图片缓存。加载或解码失败显示 alt 与原因，不再退化成无意义的小图标。
- 新增 `docs/assets/markdown-image-test.svg` 并把真实本地图片语法加入 Markdown 完整性测试文档；新增 `specs/markdown-image-rendering.md`，同步 README 和根目录进度文档。
- 新增 3 个回归测试覆盖独立/行内/代码图片结构、编码中文相对路径、Vault 根路径、远程 URL、越界拒绝及缓存键；当时的“活动行图片源码”断言现已改为验证图片在光标进入时继续保持渲染。其余图片回归继续有效。
- 按产品要求没有启动应用、没有截图、没有运行自动 UI 操作；本地/远程图片的实际加载尺寸、动画和失败占位由产品手动验收。

### 2026-08-11 — 修复远程图片网络客户端与剪贴板图片粘贴

- 根据产品手动验收继续定位远程图片失败：GPUI `Application` 默认安装的是 `NullHttpClient`，原先虽然把 HTTP(S) URL 交给了 `img`，所有网络请求仍会固定返回 “No HttpClient available”；这是真实根因，不是图片语法或解码格式问题。
- 新增 `SynapseHttpClient` 并在应用启动时通过 `with_http_client` 注入；底层使用 GPUI 同版本依赖链的 Rust Reqwest 客户端，支持系统根证书、HTTPS、HTTP/2、最多 10 次重定向、30 秒超时和明确的 Synapse User-Agent，响应继续进入 GPUI 原生异步资源/解码缓存。
- 编辑器粘贴处理从“只读取 ClipboardItem 文本”扩展为优先识别 `ClipboardEntry::Image`；图片按剪贴板声明的原始格式保存，不执行有损重编码。
- 粘贴图片保存在当前笔记所在目录的 `assets/` 子目录，自动创建目录并插入 `![Pasted image](assets/...)`；文件名使用毫秒时间戳，同名时追加 `-1`、`-2`，通过 `create_new` 保证绝不覆盖用户已有附件。
- 空图片、没有活动笔记、目录创建或写入失败会作为明确错误写入会话状态；文本剪贴板的换行规范化与替换选区行为保持不变。
- 新增 HTTP 客户端初始化/User-Agent 与附件落盘、Markdown 路径、碰撞去重、格式扩展名回归测试；当前 workspace 共 118 个自动化测试通过，另有 1 个手动性能探针按设计忽略。
- 按产品要求没有启动应用、没有截图、没有运行自动 UI 操作；远程站点实际可达性、系统剪贴板图片来源和最终视觉由产品手动验收。

### 2026-08-11 — 修复远程图片请求缺少 Tokio reactor 导致进程崩溃

- 产品粘贴 `![ERROR](https://images.waer.ltd/img/ERROR.jpg)` 后复现到明确 panic：`there is no reactor running, must be called from the context of a Tokio 1.x runtime`。
- 根因是 GPUI 使用自身非 Tokio 异步执行器轮询 `HttpClient::send`，而上一轮直接在该 Future 内执行 Reqwest/Hyper；客户端虽然能构造和编译，但真正连接网络时必须存在 Tokio I/O reactor，因此请求一开始就在后台线程 panic 并最终 abort 整个进程。
- `SynapseHttpClient` 现在拥有一个命名为 `synapse-http` 的单工作线程 Tokio 1.53.1 runtime；Reqwest 客户端在 runtime context 中构造，每次连接、TLS、重定向、超时和响应读取都通过 `runtime.spawn` 执行，GPUI 侧只等待线程安全的任务结果。
- Tokio 任务 panic 或 JoinError 会转换为普通图片加载错误返回，不再沿 GPUI 后台线程二次 panic/abort；原有 GPUI 图片缓存、HTTPS、重定向、超时和 User-Agent 行为保持不变。
- 新增本地 TCP HTTP 服务回归测试，并刻意使用非 Tokio `futures::executor::block_on` 调用客户端；测试成功收到 `200 OK` 和完整响应体，直接覆盖本次 reactor 缺失路径。
- 专项 reactor 回归及完整非视觉回归均已通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、119 个自动化测试、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图或执行自动 UI 操作；最终网络图片显示由产品使用原崩溃语法手动复验。

### 2026-08-11 — 激活页签单层表面与分段底边修复

- 产品手动验收指出首次实现仍能看到标题栏底部分割线，并且“激活底色、中央页签、左右接缝”多层背景在圆角后方发生泄漏；确认此前用复杂叠层模拟反向接缝的方案不符合实际视觉结果。
- 完整删除五层 `active_tab_surface` 装饰结构；激活页签现在就是原有页签节点自身，不再嵌套中央表面、左右上层遮盖或底部翼片。
- 激活页签自身仅增加顶部、左侧和右侧 1px 边框及 8px 顶部左右圆角，背景继续由已有 140ms 激活态颜色过渡直接控制，不存在第二套固定背景覆盖动画。
- 进一步删除标题栏父容器的贯穿式 `border-bottom`，不再依赖任何遮罩补偿；左侧系统留白、侧栏按钮、每个未激活页签和右侧拖拽区分别绘制自身底边，激活页签结构从源头没有底线。
- 激活页签节点没有任何背景子元素；圆角后方只能看到标题栏真实背景，不可能再泄漏第二套页签表面。该调整不改变标题、脏状态、页签点击及 40px 关闭按钮。
- `make-interfaces-feel-better` 的 Surfaces 规范用于撤销不必要的嵌套表面，保留单一圆角层级和作为布局边界的真实分割线。
- 8px 回归测试改为明确锁定顶部圆角，同步 README、P2 编辑器规格和根目录进度文档。
- 最终非视觉回归通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、120 个自动化测试、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图或执行自动 UI 操作；单层页签圆角和底线断开效果由产品手动复验。

### 2026-08-11 — 激活页签底部圆角方向修正

- 产品进一步澄清圆角目标是激活页签的左下角和右下角，并非左上角和右上角；上一轮对“左右转角”的方向理解错误。
- 保留已经修正的单一页签表面和分段标题栏底边结构，不重新引入背景子层、底线遮罩或左右翼片。
- 激活页签的 8px 圆角从 `rounded_tl/rounded_tr` 改为 `rounded_bl/rounded_br`；顶部两个角恢复直角，底部左右边框以圆角收尾。
- 回归测试名称和 README、P2 编辑器规格、根目录进度文档同步改为“底部圆角”。
- 最终非视觉回归通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、120 个自动化测试、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图或执行自动 UI 操作；左下和右下圆角的最终视觉由产品手动复验。

### 2026-08-11 — 激活页签底线向内圆角接缝修正

- 产品再次明确目标不是给页签矩形添加外凸的左下、右下圆角，而是让页签两侧的标题栏底线分别以向内圆弧过渡到页签垂直侧边；上一轮 `rounded_bl/rounded_br` 方案已撤销。
- 保留分段标题栏底边：激活页签内部没有底线，左侧控制区、未激活页签与右侧拖拽区继续各自绘制底边，从结构上避免贯穿激活页签。
- 新增单个非交互 GPUI Canvas，在激活页签背景上绘制标题栏色的左右外侧区域，并用一条连续 1px 路径完成“左底线 → 8px 内凹曲线 → 左侧边 → 顶边 → 右侧边 → 8px 内凹曲线 → 右底线”；页签顶部保持直角。
- 激活页签节点仍是唯一的编辑器表面色，不嵌套中央背景容器；Canvas 只负责接缝外形和轮廓，因此不会再出现圆角后方泄漏另一层页签背景的问题。
- 未激活页签若紧邻激活页签左侧，会省略自身右分隔线，避免该直线穿过左侧内凹曲线；新增纯逻辑回归测试覆盖相邻项、非相邻项与索引溢出边界。
- `make-interfaces-feel-better` 的 Surfaces 规范用于保持单一表面层级，并以连续轮廓表达层级关系；README、P2 编辑器规格和根目录进度文档均同步为内凹接缝语义。
- 最终非视觉回归通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、121 个自动化测试、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图或执行自动 UI 操作；内凹接缝的最终视觉由产品手动复验。

### 2026-08-11 — 移除页签圆角与侧栏开关容器

- 产品决定取消激活页签的全部圆角效果，只保留“激活页签底部不显示分割线”；同时指出标题栏左侧的侧栏展开/收起图标不应显示圆角按钮容器。
- 完整删除内凹接缝的半径常量、GPUI PathBuilder/Canvas 遮罩和轮廓路径、相邻页签特判及两条仅服务于圆角方案的测试；页签不再存在任何装饰绘制层。
- 标题栏继续采用分段底边：左侧系统区域、侧栏开关、未激活页签和右侧拖拽区绘制底线，激活页签不绘制底线；所有页签恢复普通直角外形和一致的右侧分隔线。
- 侧栏开关从带 hover 背景的 `ghost` 按钮改为透明 `text` 变体并显式使用 `ButtonRounded::None`，界面只显示 Lucide `panel-left` / `panel-right` 图标；40px 命中区域、Tooltip 和原点击行为保持不变。
- `make-interfaces-feel-better` 的 Surfaces 与 Minimum Hit Area 规范用于移除无意义的可见容器，同时保留不会缩小或重叠的操作区域；README、P2 编辑器规格和根目录进度文档已同步。
- 最终非视觉回归通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、119 个自动化测试、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图或执行自动 UI 操作；激活页签底线和无容器侧栏图标由产品手动复验。

### 2026-08-11 — 修复侧栏开关四周边框泄漏

- 产品截图显示侧栏开关在普通状态仍出现完整矩形边框，鼠标悬停后反而只剩图标；确认不是 Lucide SVG 自带边框，而是标题栏底线被直接设置在 GPUI `Button` 上导致的组件样式冲突。
- GPUI Component 的 Button 默认定义四条 1px 边缘；此前对按钮调用 `border_b_1().border_color(tab_border)` 时，边宽只指定底部但颜色会应用到默认四边，普通状态因此显示矩形；hover 状态又把 text 变体边框色还原为透明，造成前后反转。
- 新增一个无背景、无圆角、非交互的 40px 标题栏区域承载唯一的底部分割线；内部 Button 删除所有布局边框与边框颜色，只保留透明 text 变体、`ButtonRounded::None`、Lucide 图标、Tooltip 和点击处理。
- 现在按钮普通、悬停和按下状态都不会出现四周边框或背景容器；40×40px 命中区域没有缩小，底部标题栏分割线仍与相邻区域连续。
- `make-interfaces-feel-better` 的 Surfaces 规范用于把布局分隔线与按钮表面职责分开，并继续遵守 Minimum Hit Area；根目录进度文档已同步。
- 最终非视觉回归通过：两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、119 个自动化测试、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图或执行自动 UI 操作；图标普通与悬停状态由产品手动复验。

### 2026-08-11 — Rust + GPUI 原生磁性短线笔记目录

- 阅读参考文章及其公开源码后，将目录规则映射为现有桌面架构：只收集 H1–H3，标题不足两项时隐藏，默认短线等长，当前章节只加深颜色，悬停项及相邻项按索引距离形成磁性长短梯度。
- 新增独立 `document_outline.rs` 模块，集中管理标题模型、活动章节计算、响应式布局、磁性样式、缓动曲线和 GPUI 元素渲染；没有引入 DOM、HTML/CSS、JavaScript、WebView 或新的 Cargo 依赖。
- 目录标题直接复用 Writ/tree-sitter Markdown 呈现结果，并通过附加空行上的虚拟光标读取无源码标记标题；保留真实文档行号，支持 ATX、Setext、重复标题和 Unicode，忽略 H4–H6 与代码围栏内的伪标题。
- `EditorRenderCache` 按 Vault、笔记路径和文档 revision 缓存目录；纯光标移动、主题刷新和目录悬停不会重复解析全文。
- 原生变高 `ListState` 的滚动监听通过弱应用实体回写首个可见行，活动项始终取已经滚过的最后一个标题；点击目录项调用 `ListState::scroll_to` 精确跳到对应 Markdown 行，并阻止事件继续传入编辑器。
- 默认刻度为 6.08×1.5px、0.36 透明度；悬停中心和 1/2/3+ 邻域使用 27.52/18.88/12.16/6.08px 衰减，并用原生可中断的 180ms `cubic-bezier(0.2, 0, 0, 1)` 过渡。整个目录承接 mouse-leave，避免行间隙造成悬停闪烁；中心项显示 224px 标题卡片。
- 目录按编辑器实际可用区域定位：以正文中心右移 440px 为首选位置，同时为标题卡片执行右边界限制；编辑区小于 1280px 时隐藏，超长目录会在 70vh/576px 高度预算内压缩行距并保持垂直居中。
- 参考实现中的 Web Audio 提示音未移植，避免为了非核心反馈引入 Web 技术或额外音频运行时；该架构边界及验收条件已写入 `specs/native-document-outline.md`，README 和本进度文档同步更新。
- 新增 4 个回归测试，覆盖 H1–H3 提取与代码围栏排除、滚动活动章节、磁性距离衰减、密集目录布局、水平边界和自定义缓动；最终非视觉回归通过格式化、workspace/all-targets 严格 Clippy、123 个自动化测试、开发构建和 `git diff --check`，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图、未运行自动 UI 操作；目录的最终位置、手感和视觉由产品使用真实笔记手动验收。

### 2026-08-11 — 修复磁性目录侵入笔记内容区

- 产品手动验收确认目录刻度仍显示在笔记内容内部；根因是直接照搬参考项目的“编辑区中心 + 440px”，但 Synapse 正文可用宽度更大，该固定偏移在当前布局中仍小于正文右边缘。
- 水平定位改为使用 `editor_page_content_width` 计算正文真实右边缘，目录 40px 刻度栏固定从正文右侧 16px 之后开始，不再用编辑器中心作为内容边界的替代值。
- 标题卡片移动到刻度栏右侧 8px，并根据实际剩余空白在 160–224px 范围自适应；窗口右侧固定保留 8px 安全距离。默认窗口且侧栏展开时，正文右边缘为 1308.5px、目录起点为 1324.5px、标题卡片右边缘为 1553px，全部位于右侧空白区域。
- 当正文右侧无法同时容纳 16px 内容间距、40px 刻度栏、8px 卡片间距、至少 160px 标题卡片和 8px 窗口安全距时，目录整体隐藏；删除原先“空间不足便向左限制”的行为，避免目录再次覆盖正文、选区或编辑器鼠标命中区域。
- 更新目录规格 AC-8、README 和当前功能说明；现有布局回归测试补充默认窗口正文边界、卡片右边界、窄空白隐藏与宽屏完整 224px 卡片断言。
- 最终非视觉回归通过两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、123 个自动化测试、开发构建和 `git diff --check`，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图、未运行自动 UI 操作；目录最终视觉位置由产品手动复验。

### 2026-08-11 — 侧边栏行尾操作统一右对齐

- 产品截图指出 Todo、Bookmarks 行尾加号仍与菜单名称挨在一起，没有贴齐侧边栏右侧内容边缘。
- 根因是 `gpui-component` Button 会把自定义 children 放进按内容收缩的内部 label 容器；内部文本即使设置 `flex_1`，也只能占用该收缩容器的宽度，无法把后方图标推到整行末端。
- 两行改为明确的左右操作分区：左侧快捷按钮使用 `flex_1 + min-width: 0` 占据剩余宽度，只包含菜单图标和名称；右侧加号改为独立固定 40×40px 按钮，图标在该区域居中并自然贴齐侧边栏右边缘。
- Todo、Bookmarks 主入口和对应加号使用不同 Element ID 与互不重叠的点击区域，现阶段继续连接已有命令面板行为，为后续分别接入新建待办/书签预留独立事件入口。
- 搜索框的 `⌘K` 此前也受同一个收缩 label 容器影响；搜索内容行现在使用精确的 208px 可用宽度，即 248px 侧边栏扣除左右各 8px 外边距和左右各 12px 输入框内边距。左侧放大镜与提示文本占据剩余空间，快捷键徽标稳定贴齐输入框右侧内边缘。
- `make-interfaces-feel-better` 的 Minimum Hit Area 规则用于把原 34px 行提升到 40px，并新增常量断言锁定快捷行高度和右侧操作宽度。
- 同一回归断言同时锁定搜索框 8px 外边距、12px 内边距和 208px 内容宽度，避免后续组件内部布局变化再次把快捷键推回文案旁边。
- 最终非视觉回归通过两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、123 个自动化测试、开发构建和 `git diff --check`，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图、未运行自动 UI 操作；最终左右对齐效果由产品手动复验。

### 2026-08-11 — 原生待办工作区与标签管理

- 按产品截图新增编辑器区域级 `WorkspaceView::Todo`：点击侧边栏“待办”主区域进入独立待办工作区，右侧 40px 加号继续保留原独立回调，不会误触发视图切换；命令面板 Open Todo 同步连接新视图。
- 待办视图激活时，笔记页签与文件树笔记不再继续显示选中状态；点击文件树笔记、笔记页签或新建笔记会恢复 `WorkspaceView::Note` 和原生编辑器焦点。
- 待办工具栏复用原 40px 面包屑层级：左侧直接显示“待办”，右侧使用 Lucide 1.27.0 官方 `tag` 图标和“新建标签”；新增 SVG 编译进 `SynapseAssets`，不依赖运行目录或网络。
- 点击“新建标签”会在原位置切换为 300px GPUI Component 原生 Input，支持中文 IME；输入框 suffix 内提供两个互不重叠的 40px 勾选/X 按钮，分别执行确认和取消。
- 新增独立 `todo_workspace.rs`：标签模型支持首尾空白清理、48 字符上限、不区分大小写去重、递增稳定 ID、六色循环标记、选中筛选状态与使用计数接口；新标签确认后立即进入左侧列表并成为当前筛选项。
- 标签名称使用简单可审计的逐行文本格式写入平台用户配置目录 `Synapse/todo-tags`，重启后按原顺序恢复并默认选择“全部”；保存失败不会丢失本次内存标签，而是在待办区域显示明确错误。
- 主体使用编辑器主题表面、900px 最大居中宽度、168px 标签栏、48px 栏间距和右侧弹性内容区；左侧显示小字号灰色“标签”、全部/标签计数和空标签提示，右侧显示引导文案、添加待办占位和空状态。具体待办增删改与标签关联明确留待后续阶段。
- `make-interfaces-feel-better` 的 Minimum Hit Area 与 Surfaces 规则用于所有筛选行、工具栏操作和输入框 suffix；没有增加嵌套装饰表面或无法中断的入场动画。
- 新增 `specs/todo-workspace.md` 和 2 个模型/持久化回归测试，覆盖中文名称清理、空值、长度、重复、选中状态及配置往返；最终非视觉回归通过两个 Synapse 包格式化、workspace/all-targets 严格 Clippy、125 个自动化测试、开发构建和 `git diff --check`，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图、未运行自动 UI 操作；待办工作区最终布局和输入手感由产品手动验收。

### 2026-08-11 — 将“添加待办”占位改为可用的原生输入与任务闭环

- 产品验收指出右侧“添加待办…”只是静态占位文本，无法输入任何内容；确认这是上一轮明确遗留的功能缺口，而不是输入焦点或中文 IME 故障。
- 对照 Markd 的待办页面行为，将占位行替换为 GPUI Component 原生单行 `InputState`：输入表面透明、整行可点击编辑，左侧使用独立 40px 布局槽承载 20px Lucide `plus`，进入待办工作区后自动聚焦。
- 输入框支持中文 IME；按主回车会去除首尾空白、拒绝空内容和超过 500 字符的内容，成功后清空输入、保持焦点并把新待办插入列表顶部。发生校验或保存错误时显示明确中文信息，继续输入会自动清除旧错误。
- 新待办在具体标签筛选下创建时自动关联当前标签，在“全部”下创建时保持无标签；左侧“全部”和标签计数不再写死为 0，而是根据实际待办及标签关系即时计算，切换标签会真实过滤列表。
- 新增真实待办行与 40×40px 复选框命中区；点击可切换完成状态，并即时显示勾选、弱化文字和删除线。标签关联使用轻量胶囊标识，空状态只在当前筛选确实无记录时显示。
- 待办使用独立、可审计的制表符文本格式写入平台配置目录 `Synapse/todo-items`，记录稳定 ID、完成状态、标签名称和文本；反斜杠、制表符、换行和回车均显式转义，损坏行会被跳过，应用重启后保持列表顺序和完成状态。
- 沿用纯 Rust + GPUI 架构，没有引入 WebView、HTML/CSS、JavaScript 或新依赖；`make-interfaces-feel-better` 的 Surfaces 与 Minimum Hit Area 规则用于保持单一输入表面、稳定布局和可点击尺寸。
- 新增 2 个待办模型/持久化回归测试，覆盖中文内容清理、空值、长度、标签关联、真实计数、完成切换、转义格式、顺序和递增 ID；规格与 README 已同步。
- 最终非视觉回归通过：两个 Synapse 包格式检查、workspace/all-targets 的 127 个自动化测试、严格 Clippy、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图、未运行自动 UI 操作；输入、中文 IME、回车创建和最终视觉由产品手动验收。

### 2026-08-11 — 待办悬停操作、多标签浮层、复制与彻底删除

- 产品手动验收发现添加待办输入框左侧没有显示加号；根因是 Lucide SVG 的 `currentColor` 在该 GPUI 节点中没有继承父容器颜色。20px `plus` 现在直接设置主题弱化前景色，和此前菜单图标的修复方式保持一致，浅色与深色主题均不再依赖隐式继承。
- 详细阅读 Markd 的 `TodosPage`、`TagPicker`、`TagList`、`CopyButton`、Todo store 与 Rust 持久化实现，并将其行为映射为纯 Rust + GPUI：待办行无项目分割线，悬停显示标签/复制/删除操作，标签面板保持打开以连续多选，标签胶囊可单独移除。
- 待办行删除原有 `border-bottom`，普通、悬停和标签面板打开状态都保持编辑器原背景，不绘制灰色矩形或圆角卡片；右侧三个操作各自保留 40×40px 独立命中区，使用 Lucide `tag`、`copy`、`x`，通过 GPUI 原生 `group/group_hover` 直接控制可见性。标签面板打开时操作区持续可见，避免鼠标移向浮层后工具栏消失。
- 新增窗口级右对齐标签浮层，锚定在分配标签图标下方并限制在 8px 窗口安全边距内；面板使用独立阴影表面、最多 256px 滚动高度，逐行显示标签色圆点、名称和勾选状态。选择项目不会关闭面板，可连续分配或取消多个标签；点击外部区域关闭。
- 待办模型从单个 `Option<String>` 标签升级为有序、去重的 `Vec<String>`；当前筛选自动分配、左栏真实计数和标签筛选全部按多标签关系计算。每个已分配标签以带主题色背景/边框的 `#标签` 胶囊显示，右侧 X 只取消当前关系。
- 复制操作使用 GPUI 系统剪贴板直接写入待办纯文本；删除操作从内存列表彻底移除并立即写回 `Synapse/todo-items`，同步清理悬停与标签浮层状态，列表、筛选和计数即时更新。
- `todo-items` 新增 `v2:` 长度前缀多标签编码，能安全保存中文与转义内容，同时继续识别旧版单标签字段；升级后现有用户数据无需手工迁移。新增 2 个回归测试并扩展原持久化测试，覆盖多标签分配/取消、筛选计数、删除、多标签往返和旧格式兼容；6 个待办模块测试已通过。
- `make-interfaces-feel-better` 的 Minimum Hit Area、Surfaces 与 Performance 规则用于保持独立命中区、无永久卡片边框、浮层阴影层级，并避免悬停时触发应用级重绘；没有引入 WebView、HTML/CSS、JavaScript 或新依赖。
- 最终非视觉回归通过：两个 Synapse 包格式检查、workspace/all-targets 的 129 个自动化测试、严格 Clippy、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图、未运行自动 UI 操作；加号、悬停工具栏、浮层位置和胶囊视觉由产品手动验收。

### 2026-08-11 — 修复待办悬停操作图标始终不可见

- 产品手动验收确认待办行悬停后仍没有出现标签、复制、删除三个操作，说明上一轮虽然实现了按钮和回调，但显隐机制没有在真实子控件命中场景下生效。
- 根因是上一版依赖行节点 `on_hover` 将目标 ID 回写到 `SynapseApp`，再通过 `cx.notify()` 整页重绘以解除操作区的 `invisible`；当鼠标实际命中复选框、文本或其他子节点时，这条父行状态回写链并不稳定，操作区因此一直保持隐藏。
- 删除 `todo_hovered_id` 应用状态、悬停回调和相关重绘逻辑，改用 GPUI Component 自身已采用的 `group/group_hover`：每条待办行建立唯一 hover group，操作区在父行或任意后代命中时直接执行 `visible + opacity(1)`，不再等待应用状态更新。
- 标签面板打开时继续独立强制操作区可见，鼠标从行内移向窗口级浮层不会导致三个图标消失；三个 40×40px 命中区和标签/复制/删除原业务回调均未改变。
- `make-interfaces-feel-better` 的 Minimum Hit Area 与 Performance 规则用于保留无重叠命中区并移除不必要的悬停整页重绘；规格已同步锁定必须使用原生 group-hover 语义。
- 待办模块编译检查和 6 个模型/持久化测试已通过；最终非视觉回归通过两个 Synapse 包格式检查、workspace/all-targets 的 129 个自动化测试、严格 Clippy、开发构建和 `git diff --check`，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图、未运行自动 UI 操作，最终悬停可见性由产品手动复验。

### 2026-08-11 — 移除待办行悬停灰色矩形背景

- 产品补充明确交互细节：鼠标进入待办条目时，整行不能出现灰色矩形背景，只显示右侧标签、复制、删除三个操作图标。
- 删除待办行节点上的 6px 圆角和 `theme.active.opacity(0.35)` hover 背景，行在普通、悬停、标签浮层打开三种状态下始终直接使用编辑器背景，不再形成额外卡片表面。
- 保留 GPUI 原生 `group/group_hover` 显隐、三个 40×40px 操作命中区、面板打开时操作常驻、多标签浮层、复制和删除回调，改动不影响任何功能或布局宽度。
- `make-interfaces-feel-better` 的 Surfaces 规范用于移除无必要的嵌套表面；README、待办规格和根目录进度文档已同步。
- 最终非视觉回归通过：两个 Synapse 包格式检查、workspace/all-targets 的 129 个自动化测试、严格 Clippy、开发构建和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略。
- 按要求未启动应用、未截图、未运行自动 UI 操作；最终悬停视觉由产品手动复验。

### 2026-08-11 — 待办一键清除已完成项

- 在右侧引导文案的同一水平行最右侧新增原生 GPUI ghost 文本按钮“清除已完成 · N”，N 直接来自完整待办集合的实时完成数，不受当前标签筛选影响；完成数为 0 时按钮不显示。
- 文案区域使用弹性宽度，操作固定靠右且保留 40px 高命中区；按钮没有常驻边框或额外卡片表面，动态数量保持稳定的右侧布局。
- 新增模型级完成数统计与批量清除：一次保留全部未完成项、删除全部完成项，并自动关闭属于被清除项目的标签浮层；随后立即写回 `Synapse/todo-items`，标签计数与当前筛选列表随模型同步刷新。
- 新增回归测试覆盖多个完成项一次删除、未完成项保留、完成数归零、标签计数更新和重复清理幂等；规格与 README 已同步。
- 使用 `make-interfaces-feel-better` 的命中区、动态数字与表面层级规范；按要求不会启动应用、截图或运行自动 UI 测试，最终视觉由产品手动验收。
- 最终非视觉回归通过：7 个待办模型测试、workspace/all-targets 的 130 个自动化测试、严格 Clippy 和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略；全程未启动软件或执行截图测试。

### 2026-08-11 — 文件树名称字体与 Markd 源码对齐

- 直接核对 Markd `src/styles.css`、`src/components/tree/FileTree.tsx` 与 `package.json`：其文件树使用 Inter Variable、13px 字号、30px 固定行高；普通文件继承 400，文件夹使用 500；默认名称为 muted，激活或悬停为 ink。
- Synapse 新增 Google Fonts/Inter 官方 `opsz,wght` 可变 TTF，并在 GPUI 初始化时从编译期资源直接注册。字体、SHA-256、上游修订、Markd 使用的 Fontsource 版本及 SIL OFL 1.1 许可证均随仓库记录；运行时不访问网络，也未引入 WebView、CSS 或 JavaScript。
- 文件与文件夹行统一改用 `Inter`、13px 和 30px；文件显式使用 400 字重，文件夹显式使用 500 字重。默认名称改为主题 muted，激活/悬停改为主题 ink；删除折叠文件夹整行 `0.82` 透明度，避免同级名称因展开状态出现参考项目没有的色差。
- 内联重命名输入继续继承所在树行的字体、字号和字重，并按 Markd `RenameInput` 使用 ink 文字色，进入编辑状态不会跳回系统字体或继续显示 muted；中文字符由 GPUI 字体回退处理，与 Markd 的 Inter + 系统字体栈语义一致。
- 新增常量回归测试锁定字体族、13px、30px 以及浅色/深色 muted/ink 色值；使用 `make-interfaces-feel-better` 的字体平滑与文字层级规范校验，按要求不启动软件或执行截图测试。
- 最终非视觉回归通过：workspace/all-targets 的 131 个自动化测试、严格 Clippy、Synapse 包格式检查和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略；全程未启动软件或执行截图测试。

### 2026-08-11 — 侧边栏展开收起动画对齐 Markd

- 深入核对 Markd `AppShell.tsx`、`ResizableSidebar.tsx` 与 `ease.ts`：侧栏外层宽度在当前宽度和 0 之间使用 `SPRING_PANEL` 动画，内部内容保持固定宽度并由外层 `overflow-hidden` 裁切，主编辑区依靠 flex 同步伸缩；标题栏左侧 padding 使用同一弹簧联动，首次渲染不播放动画。
- Synapse 将原本直接切换 248px/0px 的单层侧栏拆为固定 248px 内容层和独立裁切视口。内容层始终挂载，动画过程中不会把搜索框、文件树和底部设置栏挤压换行；视口宽度变化会让编辑工作区即时占回或释放真实布局空间。
- 新增 `MarkdPanelSpring` GPUI transition，复用 Markd 的 stiffness 420、damping 40、mass 0.5 过阻尼响应，并按项目动画规约归一化到 180ms；没有手写定时循环，也没有引入新的动画依赖。GPUI transition registry 保留当前插值状态，因此快速反向点击可从当前位置继续。
- macOS 标题栏左侧安全边距从 10px 到 84px 使用同一 transition 与稳定 ID，侧栏和页签区域同步滑动；首次渲染由 transition 初始目标状态直接建立，不产生启动飞入。
- 动画规约已同步记录这一产品指定特例；新增回归测试锁定 180ms、三项 Markd 弹簧参数、端点、单调性和无过冲。使用 `make-interfaces-feel-better` 的可中断动画与明确属性原则，按要求不启动软件或执行截图测试。
- 最终非视觉回归通过：workspace/all-targets 的 132 个自动化测试、严格 Clippy、Synapse 包格式检查和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略；全程未启动软件或执行截图测试。

### 2026-08-11 — 待办标签选中与新建标签输入框动画对齐 Markd

- 对照 Markd `TagRail.tsx` 与 `AppShell.tsx`：标签选中态的核心是共享 `layoutId` + `SPRING_LAYOUT` 的绝对定位激活 pill，在标签行之间滑动而非原地闪变；新建标签按钮与输入框通过 `AnimatePresence` 交叉淡化，输入框为无边框透明样式，内部右侧是 Check/X 两个圆形 hover 图标按钮。
- Synapse 将标签列选中态从逐行 `bg(active)` 改为唯一绝对定位 pill：pill 以 6px 圆角、`theme.active` 背景覆盖当前行，`top` 随激活索引驱动 `TagPillSpring` 过渡（stiffness 360、damping 32、mass 0.6 过阻尼响应，按规约归一化 180ms）；行高 40px + 2px 间距形成稳定步长，pill 不参与文档流，按钮文字与计数保持在 pill 之上。
- 选中行文字由 muted 切为 `ink`，计数从固定 muted 改为选中 muted / 未选中 faint，与 Markd 的 active/muted/faint 层级一致；pill 带稳定唯一 transition ID，快速连续点击标签会从当前插值位置继续滑动。
- 新建标签按钮改为常驻双态容器：外层宽度在 104px 与 240px 之间以 180ms `EaseInOutCubic` 过渡，输入框与按钮分别持有独立 transition ID，以 140ms `EaseOutQuad` 交叉淡化；关闭态按钮层（后渲染）覆盖整个容器拦截命中，打开态按钮层瞬时 `invisible` 释放输入框区域，透明度动画全程不被命中区截断。
- 输入框改用 `Input::appearance(false)` 无边框无背景并关闭 focus 边框，右侧勾选/取消改为两个 24px 圆形 hover 按钮（图标 13px、faint 色），不再出现组件默认边框；新增 Enter 提交与 Esc 取消（Esc 冒泡到待办工作区容器处理并停止传播），对齐 Markd 的键盘交互。
- 动画规约已同步记录 `TagPillSpring` 特例与新建标签展开参数；新增回归测试锁定 180ms、三项 Markd 弹簧参数、端点与单调性。使用 `make-interfaces-feel-better` 的可中断动画与明确属性原则，按要求不启动软件或执行截图测试。
- 最终非视觉回归通过：workspace/all-targets 的 133 个自动化测试、严格 Clippy、Synapse 包格式检查和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略；全程未启动软件或执行截图测试。

### 2026-08-12 — 侧边栏待办快捷列表对齐 Ornata

- 基于当前未提交的待办侧栏预览代码，直接核对 Ornata `src/components/layout/SidebarCollections.tsx`：参考实现用完整 `todos` 映射列表，完成项继续保留；集合行把左侧导航 button 与右侧展开 button 分开，展开面板使用 150ms 高度/透明度过渡。
- Synapse 将右侧原本由两条 GPUI 矩形自绘的加减图形替换为 Lucide 1.27.0 官方 `plus` / `minus` SVG；新增 `minus.svg` 已编译进 `SynapseAssets` 并纳入全图标资源完整性测试，图标显式读取当前主题色。
- 待办集合行收敛为 30px 高度，右侧仍保留独立 40px 宽命中区并贴齐边缘；左侧 `todo-collection-nav` 只调用 `open_todo_workspace`，右侧 `todo-collection-toggle` 只调用 `toggle_todo_quick_picker`，不再打开命令面板，也不会因进入待办工作区或点击列表外部隐式收起。
- 侧边栏预览数据由“仅未完成”改为完整集合；勾选后条目数量和原有顺序保持不变，完成项显示实心复选框、Lucide check、faint 文字和删除线，再次点击可恢复为未完成并立即持久化。
- 新增模型回归测试锁定完整列表对完成项的保留、顺序和状态；README、待办规格、Lucide 来源记录与本进度文档同步。使用 `make-interfaces-feel-better` 的独立命中区、官方图标和明确状态反馈原则；没有引入 WebView、HTML/CSS 或 JavaScript。
- 最终非视觉回归通过：workspace/all-targets 的 134 个自动化测试、严格 Clippy、Synapse 包格式检查和 `git diff --check` 全部成功，1 个手动性能探针按设计忽略；全程未启动软件或执行截图测试。

### 2026-08-13 — 修复斜体字形、连续编辑延迟与细弱光标

- 直接核对本机与远程 `ornata-new` 同源的 `/Users/xuyi/Documents/Ornata-markd`：参考编辑器以 Inter Variable 加载独立 Italic 字体文件，正文和光标均使用主题 `ink`；CodeMirror 光标约 1.2px。Synapse 原解析链已经生成 italic style run，但此前只内置 Normal 字体，GPUI 无法稳定合成斜体，因此菜单写入语法后视觉上仍像普通文本。
- 新增 Google Fonts 官方 `Inter-Italic[opsz,wght].ttf`，与 Normal 可变字体一起在启动时注册；正文行显式使用 `Inter`。保留标准 `*文本*` Markdown 数据格式，并新增呈现回归确认标记隐藏后仍生成 italic style run。
- 光标宽度从 1px 调整为 1.5px，并以实际 x 坐标为中心绘制，避免单向加宽产生位置偏移；颜色继续使用完整主题 foreground，即与参考项目一致的浅色 `#191919` / 深色 `#ebebe8`，不添加透明度。
- 定位删除延迟的主要同步热路径：此前每次按键都会把 NoteDocument Rope 转为 String、重新构造 Writ Buffer 并解析整篇 Markdown；自动新建笔记还会在正文每次编辑时重新提取一级标题，并可能进入文件系统路径与 Vault 条目同步。
- `EditorRenderCache` 现在持有 revision 对齐的持久 Writ Buffer；原生输入协议、中文组合输入、Backspace、Delete 和选区替换把 Unicode 字符范围直接转换为缓存 Rope 字节范围并调用 Writ 增量 replace。同步用 revision 和路径校验，不复制旧全文，也不做逐键全文比较；缓存命中后的渲染也直接从 Writ 快照逐行读取，不再先把整篇 Rope 扁平化成 String；缓存失配时安全退回一次完整解析。
- `NoteDocument` 新增基于 Rope 首行的长度查询，只有触及首行的编辑（包括 IME 组合提交）才运行标题/文件名联动；正文连续输入、删除不会再提取全文标题或扫描 Vault。目录数据直接从本帧已解析的 H1–H3 行构建，活动标题会去除可见源码标记，不再为目录执行第二次 Markdown 解析。
- 新增/扩展 4 类定向回归，覆盖真实斜体 style run、中文字符范围的 Writ 增量替换、正文编辑保持链接文件路径与目录快照、Rope 首行长度和活动标题目录清理。最终非视觉回归通过两个 Synapse 包格式检查、`cargo check -p synapse`、workspace/all-targets 的 148 项自动化测试、严格 Clippy 和 `git diff --check`；1 项手动性能探针按设计忽略。
- 按要求没有启动应用、截图或运行自动 UI 操作；快速连续删除的最终主观跟手性、斜体字形和光标视觉由产品手动验收。

### 2026-08-13 — Markdown 加粗与行内代码最终绘制初次调整

- 产品验收指出 `**加粗**` 和行内反引号代码虽然标记会被隐藏，但最终视觉没有正常体现。检查确认 Writ 已正确生成 bold/mono style run，缺口位于 GPUI 最终绘制层：加粗使用笼统的 700 `bold()`，没有对齐参考项目的 650 正文字重；行内代码只有等宽字体，没有参考项目的背景、圆角和内边距。
- 加粗 TextRun 初次改为直接设置 Inter Variable 650 字重，对齐 Ornata `.prose-note strong { font-weight: 650 }`；后续产品手动验收确认 GPUI/macOS 仍选中 Regular 字体面，屏幕上的加粗与普通文本没有可辨差异，因此这项实现已被下方“实际字体匹配修正”替代。
- 行内代码继续使用平台等宽字体（macOS 为 Menlo），并按真实 mono run 字节范围在文字下方绘制主题 `hover` 表面、4px 圆角、约 2.4px 垂直和 6.4px 水平视觉内边距；软换行时按每个视觉行分段绘制并限制在编辑器边界内。
- 行内代码最终绘制继续使用等宽字体和主题背景；当时“当前编辑行显示 muted 反引号”的策略已被持续富文本渲染态替代，默认模式始终隐藏反引号，只有右上角完整源码模式显示原始标记。围栏代码块和完整源码模式不重复套用行内背景。
- 待办正文和脚注定义复用同一个行内代码背景 token，普通正文、待办及脚注的 Markdown 呈现保持一致。
- 扩展语法回归验证加粗与 mono run；当时新增的 GPUI TextRun 回归只锁定了 650 字段和 Menlo 字体，未验证字体选择器最终选中的真实字体面，因此未能发现 Regular 回退问题。行内代码相关回归仍然有效。
- 按要求未启动应用、截图或运行自动 UI 操作；最终字重、代码胶囊视觉和编辑态切换由产品手动验收。

### 2026-08-13 — 修正 Markdown 加粗实际字体匹配

- 产品手动验收确认初次调整无效：`**加粗**` 的 `TextRun.font.weight` 虽然已经变为 650，但最终字形和普通文本完全一致。进一步检查 GPUI 0.2.2 的 macOS 文字系统，确认它使用 `font_kit::matching::find_best_match` 按注册字体面的静态 style/weight 属性选取字体，不会像浏览器 CSS 那样自动实例化 Inter 可变字体的任意 `wght` 轴；只注册 Regular 可变字体时，650 请求仍可能选回 Regular。
- 从仓库已有的 Google Fonts 官方 Inter Normal/Italic 可变字体生成同源的 weight-700 `Inter-Bold.ttf` 与 `Inter-BoldItalic.ttf`，并与 Normal/Italic 一起在 GPUI 启动阶段注册为同一 `Inter` 字族。普通粗体会精确命中 Bold，`***粗斜体***` 会精确命中 Bold Italic，不再依赖平台伪粗体或不可控的变量轴匹配。
- 最终 Markdown strong run 明确请求 700；删除线、下划线、斜体等组合样式继续叠加在同一 TextRun，不改变保存的标准 Markdown 源码。使用 macOS CoreText 对同一段“中文abc”做无窗口塑形探针：Regular 基准实际得到 `PingFangSC-Regular` 且 bold=false，Bold 基准实际得到 `PingFangSC-Semibold` 且 bold=true，确认中文后备字体也随独立粗体基准正确变粗，而不是只修复英文字形。
- 字体资源文档新增 Bold/Bold Italic 的来源、生成方式与 SHA-256；新增 SFNT/OS/2 结构回归，直接读取打包字体的 `usWeightClass` 和 `fsSelection`，确认 Bold 为 700+Bold、Bold Italic 为 700+Bold+Italic。它验证的是实际提供给 GPUI 匹配的字体面，而不再只是业务层权重字段。
- 最终非视觉回归通过加粗字体面结构、包含中文粗体/粗斜体的最终 GPUI TextRun 两项定向测试、macOS CoreText 中文字体回退探针、workspace/all-targets 的 150 项自动化测试、严格 Clippy、两个 Synapse 包格式检查和 `git diff --check`；1 项手动性能探针按设计忽略。按要求未启动应用、截图或执行自动 UI 操作，实际加粗视觉等待产品手动验收。

### 2026-08-13 — 修正 Markdown 斜体实际字形与中文强调

- 产品手动验收确认粗体已经生效，但 `*斜体*` 仍与普通文本没有可辨区别。检查打包字体和 CoreText 实际塑形结果后确认存在两个独立问题：GPUI/macOS 对 Inter Italic 可变字体仍依赖静态属性匹配；更关键的是 Inter 没有中文字形，italic run 的中文会回退到 `PingFangSC-Regular`，其 italic=false，因此即使业务层 TextRun 已经设置 Italic，中文最终仍是直立常规字形。
- 从官方 `Inter-Italic-Variable.ttf` 生成明确的 weight-400 `Inter-Italic.ttf` 并替换运行时注册的 Italic 可变字体；其 OS/2 表明确声明 400+Italic，Latin/数字/西文标点会稳定命中真实 Inter Italic，而不是再次依赖 GPUI 对变量轴的推断。
- 为 Markdown italic run 增加纯 GPUI 的跨平台 CJK 强调后备栈：macOS 普通斜体优先 `Kaiti SC`、粗斜体优先 `Kaiti SC Bold`，并保留繁体与 `STKaiti` 后备；Windows 使用 `KaiTi`，Linux 使用 Noto Serif CJK。中文字体没有原生 italic face，原生文字系统也无法通过 italic trait 生成该字形，因此采用中文排版中以楷体表达强调的惯例；没有引入 WebView、CSS、Canvas 或 JavaScript。
- macOS CoreText 无窗口塑形探针确认：`*中文 italic*` 最终拆分为 `STKaitiSC-Regular` + 真实 `InterItalic-Italic`，`***中文粗斜体***` 拆分为 `STKaitiSC-Bold` + 真实 `InterItalic-BoldItalic`；普通中文正文仍保持原有 PingFang，不会全局更换字体。组合样式继续保留粗体、下划线、删除线等既有属性。
- 新增/扩展回归直接读取 concrete Italic 的 OS/2 权重与 italic/bold 标志，并锁定最终 GPUI TextRun 的 Italic style、普通/粗斜体 CJK 后备顺序和组合样式。最终非视觉回归通过 workspace/all-targets 的 150 项自动化测试、严格 Clippy、两个 Synapse 包格式检查和 `git diff --check`；1 项手动性能探针按设计忽略。按要求未启动应用、截图或执行自动 UI 操作；最终视觉等待产品手动验收。

### 2026-08-13 — 默认富文本渲染态与显式 Markdown 源码态对齐 Ornata

- 直接核对用户指定的 `08820048/ornata-new` 当前 main（本地与远程 HEAD 均为 `821eee0`）：默认 `markdownSource=false` 时持续挂载 Tiptap 结构化编辑器，Markdown 只作为加载/保存格式；点击右上角源码按钮后才切换到独立的 CodeMirror Markdown 源码编辑器，切回时重新解析源码。参考项目不存在“光标进入一行就自动显示 Markdown 标记”的混合模式。
- Synapse 默认模式不再把真实编辑光标传给 Writ 的语法揭示策略，而是使用文档范围之外的渲染光标，保证标题 `#`、粗斜体标记、反引号、链接目标等标记始终隐藏；源 Markdown 仍由 `SegmentMap` 的 source/display 映射作为保存权威数据，鼠标定位、选区、IME、输入、删除和快捷键继续写回原始字符范围。
- 移除所有由活动光标触发的局部源码回退：Setext 下划线、`<u>` 标记、图片语法、Mermaid 围栏不再在光标进入时显露；表格、任务列表、脚注、独立/行内图片、行内/块公式、Mermaid 图表和代码围栏也不再因活动行切回源码组件。默认态持续使用其渲染组件，只有右上角 `Show Markdown source` 进入完整源码态。
- `EditorRenderCache` 的默认渲染结果不再包含 cursor 作为缓存键；只移动光标不会重新解析整篇 Markdown、重建虚拟列表或让块组件跳变。光标与选区仍由当前 source/display 映射单独绘制，源码模式切换、文档 revision、主题或路径变化仍会正确失效缓存。
- 更新回归语义，覆盖活动标题/粗体/行内代码/下划线/图片仍保持渲染、Mermaid 块在光标进入时保持单一预览锚点、显式源码模式保留逐字显示与恒等映射，以及默认渲染缓存忽略光标。使用 `make-interfaces-feel-better` 的稳定排版和避免无关布局跳变原则；没有引入 WebView、HTML/CSS 或 JavaScript，按要求未启动软件或执行截图测试。
- 默认渲染块不再因为不切回源码而丢失输入接管：活动的任务项、脚注、表格、独立/行内图片、公式与 Mermaid 预览会注册原生 GPUI `EntityInputHandler`；任务和脚注正文继续绘制光标与选区，输入仍通过 source/display 映射写回 Markdown。
- 同步更新 README 与 P2、图片、脚注/提示块/任务、P4 Mermaid、P5 数学规格，清理“光标进入后恢复源码”的旧行为描述。最终非视觉验证通过 workspace/all-targets 共 150 项自动化测试、严格 Clippy、两个本轮 Rust 文件的 2024 edition rustfmt 检查和 `git diff --check`；1 项手动性能探针按设计忽略。未启动应用、未截图或执行自动 UI 操作。

### 2026-08-13 — Markdown 格式快捷键与代码围栏快捷输入

- 对照 `ornata-new` 当前 Tiptap StarterKit/CodeBlock 实现确认其标准按键为 `Mod+B`、`Mod+I`、`Mod+Shift+S`、`Mod+E`、`Mod+Alt+C`，代码围栏输入规则支持反引号/波浪线与可选语言。Synapse 在原生 GPUI `SynapseEditor` 键盘上下文中注册对应 macOS 与 Windows/Linux 绑定，并按现有产品能力补充 `Mod+U` 下划线。
- 粗体、斜体、下划线、删除线和行内代码快捷键统一复用划词工具条的 `InlineFormatEdit`，避免两套格式逻辑分叉；非空中文/Unicode 选区可包裹与取消，空光标会插入成对标记并停在标记内部。代码块快捷键在空光标处插入完整围栏，非空选区扩展到完整行后包裹，继续保存为标准 Markdown。
- `smart_enter_edit` 在 Writ 列表/引用续写之前新增围栏结构规则：行首 0–3 空格缩进、至少三个反引号或波浪线、可选无空白语言标识会生成内容行和匹配闭合围栏；光标精确落在内容行缩进后。下方已有匹配闭合围栏时只插入内容行，内联/不完整围栏保持普通回车，避免误触发。
- 新增围栏自动闭合、已有闭合复用、无效输入拒绝、空光标成对格式标记、中文行代码块包裹等回归；同步 README 快捷键表、P3 功能规格和本进度文档。使用 `make-interfaces-feel-better` 的键盘反馈连续性原则，没有增加浮层或动画，也未引入 Web 技术。
- 修复持续渲染态与快捷输入之间的边界：当前光标行从第一个反引号/波浪线开始就暂时使用可见源码行与恒等字符映射，第一个、第二个、第三个标记以及后续 `rust` 等语言标识都会逐键显示；smart-enter 补齐闭合围栏后才进入隐藏围栏、渲染代码容器的正常状态。新增从空文档逐字符插入 `````rust`` 再执行 Enter 的会话级回归。最终 workspace/all-targets 共 157 项自动化测试通过，1 项手动性能探针按设计忽略；严格 Clippy、四个本轮 Rust 文件的 2024 edition rustfmt 检查和 `git diff --check` 全部通过。
- 按产品要求完成真实应用验收：重新构建后以独立临时 Vault 启动 Synapse，聚焦编辑器并通过 macOS 原生键盘事件逐次输入三个反引号，界面可见 `````；继续输入 `rust`、回车和 `fn main() {}` 后，编辑器立即渲染为单一代码块。`Command+S` 落盘内容精确为 `````rust\nfn main() {}\n``` ``，证明字符输入、围栏自动闭合、渲染和保存链路均已打通；测试实例已与用户原有运行实例隔离。
- 修正先前对输入场景的错误判断：问题与简体拼音无关，真实路径是 macOS 英文输入状态下按右 `Option`（空格右侧第二个键）+ 数字 `1` 左侧的波浪号键。GPUI 会把该事件保留为带 `alt` 修饰的物理 `` ` `` 键，原有仅匹配无修饰逻辑字符的绑定因此不会触发。`SynapseEditor` 现同时绑定直接 `` ` `` 与 `alt-`` `，两者统一走原生反引号插入、Writ 增量同步、选区替换和光标闪烁链路；新增绑定回归测试并同步 P3 规格。
- 使用独立临时 Vault 和重新构建的真实 Synapse 应用完成精确物理键验收：切换到 macOS `ABC` 英文输入源，向应用发送右 `Option` 键码 61 与波浪号物理键码 50，连续三次后保存文件的字节精确为 `60 60 60`；继续通过物理键输入 `rust` 并回车，最终磁盘内容为 `````rust\n\n``` ``，确认组合键输入、围栏识别、自动闭合和保存链路全部生效。测试实例已停止，并恢复测试前的系统输入源。

### 2026-08-13 — 围栏代码块键盘与鼠标退出

- 定位代码块无法退出的两个共同原因：富文本模式会把闭合围栏渲染为 0 高度，文档以代码块结尾时又不存在真实的块后段落；因此普通回车只能继续写入围栏内部，点击底部留白也只能回落到最后一个代码内容行。
- 对照 `ornata-new` 使用的 Tiptap CodeBlock `exitOnTripleEnter` 默认实现，在原生 Rust `smart_enter_edit` 中实现相同语义：代码块末尾第一次、第二次回车继续产生代码内容换行，第三次检测末尾双换行，删除两个空代码行并把光标移动到闭合围栏之后；已有后续段落时直接进入该段落，文档末尾则创建一个标准 Markdown 块后空行。
- 编辑器鼠标落点新增末尾围栏处理：点击最终代码块下方的编辑器空白区域时，按需追加一个真实普通段落并把光标放入其中；普通文档、已有块后段落和代码块内部点击不改变现有映射。实现保持 Rust + GPUI/Writ 架构，没有引入 WebView、HTML 或 JavaScript。
- 新增命令级与会话级回归，覆盖前两次回车仍在块内、第三次回车清理并退出、已有后续正文复用、末尾鼠标段落创建和非代码文本拒绝误触；同步 README、P3 规格与本进度文档。
- 重新构建后使用独立临时 Vault 完成真实 GPUI 键盘链路验收：在 macOS `ABC` 英文输入源下以右 `Option` + 波浪号物理键输入三个反引号，输入 `rust` 回车形成代码块，键入 `x` 后连续三次回车再输入块外文本；磁盘最终结构精确为 `````rust\nx\n```\n<块外文本> ``，证明第三次回车确实越过闭合围栏而非继续写入代码。鼠标探针同时确认点击最终代码块下方会先创建真实块后空行；测试实例已停止并恢复原输入源。最终 workspace/all-targets 共 135 项自动化测试通过，1 项手动性能探针按设计忽略，严格 Clippy、开发构建、格式化和 `git diff --check` 全部通过。

### 2026-08-13 — 默认 Vault、工作区持久化与设置面板重构

- 建立完整的启动工作区优先级：显式命令行路径优先，其次读取用户保存的 Vault 路径；没有设置、保存路径已失效或目录被移除时，自动创建并打开 `~/Documents/Synapse Vault`。默认目录会在首次启动时一并写入原生配置目录，软件不再以 “No vault open” 空壳状态启动，可立即在默认工作区新建文件夹和笔记。
- 工作区选择变为持久设置：现有文件夹选择器继续支持命令面板和设置入口；只有新目录成功打开后才更新配置文件，切换失败或取消不会覆盖原工作区。成功切换会清理旧目录折叠状态、重置编辑选区并重启文件系统 watcher；设置面板路径信息同步显示实际 canonical Vault 根目录。
- 对照 `ornata-new` 的设置层级，以纯 Rust + GPUI 重构设置面板：宽度调整为 460px，顶部使用独立图标、标题、副标题和 40px 关闭命中区；正文分为 Appearance 与 Workspace 两组，Vault 卡片显示等宽截断路径、文件夹图标和 “Change” 操作，不再是三张纵向测试按钮。
- 主题模式改为 252px 三段式按钮组，System / Light / Dark 共用一个 32px 活动指示块；指示块按参考项目的 420/40/0.5 spring 响应在分段之间滑动，文本和图标随选中状态切换。面板和遮罩使用独立、可中断的 220ms 进入/退出过渡，面板以轻微 7px 位移和透明度过渡呈现；遮罩通过主题自适应的半透明层弱化并视觉模糊后方内容，面板使用圆角、细边框和原生阴影建立层级，没有引入 WebView、HTML、CSS 或 JavaScript。
- 新增启动路径优先级、失效保存路径回退、主题指示块等分布局和 spring 边界回归；同步本进度文档。实现遵循 `make-interfaces-feel-better` 的最小 40px 命中区、可中断动画、明确属性过渡和背景/面板层级原则。
- 使用隔离临时 HOME 启动真实构建完成非截图验收：无任何配置和命令行参数时，应用自动创建 `Documents/Synapse Vault`，并在 `Library/Application Support/Synapse/vault` 写入同一绝对路径；证明首次启动的目录创建与持久化链路生效且没有读取或修改用户真实配置。最终 workspace/all-targets 共 137 项自动化测试通过，1 项手动性能探针按设计忽略；严格 Clippy、开发构建、格式化和 `git diff --check` 全部通过。

### 2026-08-13 — 设置面板深浅主题一致性修复

- 定位设置面板切换深色模式后出现白色主体与黑色控件混用的根因：切换动作中组件库按钮已经读取新的全局主题，而面板自绘容器仍持有当前渲染帧开始时克隆的旧主题快照，造成同一帧两套主题来源并存。
- 设置面板改为根据刚选中的 System / Light / Dark 偏好独立解析即时外观，并从统一的 Synapse 调色板显式取得全部面板颜色；System 继续读取系统当前外观，Light / Dark 不再受旧渲染快照影响。
- 深色面板统一使用 `#1a1a1a` 主表面、`#151515` Workspace 卡片、`#0f0f0f` 主题分段底色、`#ebebe8` 主文字和 `#8f8f8a` 次文字；边框、图标、关闭操作、路径、Change 按钮及错误状态同步适配。浅色面板继续使用 `#fbfbfa` / `#f4f4f2` / `#e9e9e6` 对应层级。
- 新增设置表面随显式主题即时切换的回归，覆盖 System 对系统外观的映射、Light / Dark 对旧外观快照的覆盖及两套面板关键色和文本对比关系。定向 2 项设置主题测试、`cargo check -p synapse` 与定向 rustfmt 检查通过；按产品要求未启动应用、未运行截图或自动 UI 测试，等待手动验收。

### 2026-08-14 — 设置面板全局主题链路修正

- 纠正上一轮将设置面板外观按选项单独推导的错误方案：软件主题切换必须是全局操作，设置面板现已移除独立调色板与独立深浅判断，面板、Workspace 卡片、分段控件、文字、图标、按钮、边框、错误状态和遮罩全部只读取 GPUI Component 全局 `Theme`。
- 确认深色切换后设置面板仍呈浅色的真实原因是 `gpui-animation` 会缓存动画节点的完整 Style：面板、遮罩和主题指示块的浅色背景曾与 `opacity` / `translate` / `left` 一起进入动画状态，后续即使全局 Theme 已切换，旧缓存仍会覆盖新主题颜色。
- 重构设置浮层的动画/表面层级：动画节点现在只承担透明度、位移或水平位置，实际背景、边框、圆角、文字和图标位于其子表面中，每次渲染均从最新全局 Theme 取值；主题动画继续保留，不再缓存或覆盖颜色。
- `apply_synapse_theme` 在完成 GPUI Component 模式切换和 Synapse 全部自定义 token 写入后显式刷新窗口，避免窗口仅绘制组件库默认主题的中间状态。System 的系统外观监听、Light / Dark 全局切换和偏好持久化保持原逻辑。
- 全 workspace 共 164 项自动化测试通过，另有 1 项手动性能探针按设计忽略；`cargo check -p synapse` 和定向 rustfmt 检查通过。按产品要求未启动应用、未执行截图或自动 UI 测试，等待手动切换主题验收。

### 2026-08-14 — 设置面板迁移至官方 Settings 组件

- 按产品建议对照 gpui-component 官方 Settings 文档及项目当前固定的最新版 `0.5.1` 源码，将设置正文从自绘行迁移到真实的 `Settings → SettingPage → SettingGroup → SettingItem → SettingField` 组件体系；没有只复刻外观，也没有引入 WebView 或其他 Web 技术。
- 设置浮层调整为适合官方两栏结构的 820×560px：左侧使用组件自带的搜索输入和 General 页面导航，右侧使用官方页面标题、描述、滚动区域与 Outline GroupBox 分组；侧栏初始宽度为 200px并可使用组件原生调整能力。
- Appearance 使用官方 `SettingGroup` / `SettingItem` 承载 Theme 字段，字段内部保留 System / Light / Dark 三段控件和 spring 指示块，继续调用全局主题切换及偏好持久化逻辑；所有颜色仍只读取全局 Theme。
- Workspace 使用官方设置项承载当前 canonical Vault 路径和带文件夹图标的 Change 操作，继续复用原生目录选择、切换保护、文件监听重启及路径持久化逻辑。弹窗的关闭按钮、主题自适应遮罩与进入/退出动画保留。
- 使用界面细节规范复核 40px 交互命中区、动画/主题表面分层和内容可用尺寸；gpui-component crates.io 最新版本确认仍为 `0.5.1`。全 workspace 164 项自动化测试通过，另有 1 项手动性能探针按设计忽略；严格 Clippy、`cargo check -p synapse`、定向 rustfmt 和 `git diff --check` 通过。按产品要求未启动应用、未运行截图或自动 UI 测试。

### 2026-08-14 — 设置改为独立工作区（误实现，已由下一节替代）

- 根据产品截图纠正对官方 Settings 组件定位的误读：该组件本身就是完整设置工作区，不应嵌入自定义弹窗。已删除设置弹窗的固定宽高、圆角/阴影外壳、遮罩、重复 Settings 标题栏、关闭按钮以及弹窗进入/退出状态和动画。
- `WorkspaceView` 新增 `Settings`，与 `Note`、`Todo`、`Bookmark` 同级；点击侧栏底部或命令面板的 Settings 会直接将右侧编辑器区域切换成设置工作区，左侧 Synapse 主侧栏与顶部应用标题栏保留。选择笔记页签/文件、待办或书签即可切离设置，既有笔记页签、缓冲区和编辑状态不被重建。
- 官方 `Settings` 现在直接获得右侧工作区域的完整可用宽高，内部搜索、General 导航、分组导航、页面标题、设置项和滚动列表不再被 820×560px 弹窗裁切；官方设置侧栏初始宽度调整为 240px，右侧 Vault 路径最大展示宽度提升到 360px。
- Appearance 和 Workspace 继续由官方 `SettingPage` / `SettingGroup` / `SettingItem` / `SettingField` 实现，主题三段控件、全局主题切换、Vault 路径、更换目录和持久化业务保持不变。全 workspace 164 项自动化测试通过，另有 1 项手动性能探针按设计忽略；基础编译通过。按产品要求未启动应用、未运行截图或自动 UI 测试。

### 2026-08-14 — Settings 改为原生独立窗口

- 根据产品进一步澄清，上一节将 Settings 作为主窗口 `WorkspaceView` 的实现仍不符合需求，已完整撤销：`WorkspaceView` 恢复为 Note / Todo / Bookmark，主窗口不再存在 Settings 正文分支，打开设置不会切走当前笔记、待办或书签界面。
- 侧栏底部与命令面板的 Settings 入口现在通过 GPUI `open_window` 创建真正的 `WindowKind::Normal` 原生窗口；窗口使用不透明系统标题栏并显示 `Synapse Settings`，由操作系统提供关闭、最小化和缩放控制，同时明确启用窗口移动、自由调整尺寸及 760×520 最小尺寸。
- 设置窗口默认以 1000×700 居中打开，内部直接以 gpui-component `Root` 承载官方 `Settings` 两栏界面，不再绘制模态遮罩、自定义弹窗边框或关闭按钮。它与主 Synapse 窗口拥有彼此独立的尺寸和窗口生命周期。
- 主应用保留设置窗口句柄；窗口仍存在时重复点击任一 Settings 入口只会将其激活并带到前台，关闭后再次点击会清理失效句柄并创建新窗口，避免堆叠多个设置窗口。
- Settings 窗口订阅主应用状态，主题偏好、Vault 路径和持久化错误会实时更新；System / Light / Dark 仍走全局主题链路，Change 仍复用原生目录选择与 Vault 安全切换逻辑。新增定向回归锁定普通窗口类型、系统标题栏、可移动/可调整/可最小化属性及最小尺寸；全 workspace 165 项自动化测试通过，另有 1 项手动性能探针按设计忽略，严格 Clippy、两个 Synapse 包格式检查、`cargo check -p synapse` 与 `git diff --check` 均通过。按产品要求未启动应用、未执行截图或自动 UI 测试。

### 2026-08-14 — 修复 Settings 首次打开时 GPUI 重入闪退

- 根据产品提供的 macOS abort 回溯重新审计真实事件时序：崩溃发生在左键 MouseUp 回调。`SynapseApp.update` 尚未返回时同步调用 `open_window`，而 GPUI 会在 `open_window` 返回前立即绘制新窗口；Settings 首帧随后读取同一个仍处于可变更新栈中的 `SynapseApp`，触发不可重入的实体借用 panic。panic 穿过 macOS Objective-C 事件回调的不可展开边界后直接终止进程。
- 窗口创建现通过 GPUI effect-cycle `defer` 延迟到当前实体更新完整退出后执行；新窗口首帧读取共享主题、Vault 和错误状态时不再与主实体的可变更新重叠。新增 `settings_window_opening` 状态合并创建完成前的连续点击，成功或失败后统一复位。
- 使用独立 `/tmp` 临时 Vault 启动真实构建并通过 macOS 原生鼠标事件完成无截图冒烟测试：首次点击成功出现 `Synapse Settings` 第二窗口，进程保持运行；系统辅助功能确认关闭、全屏/缩放、最小化三个原生按钮全部可用；再次点击只激活现有窗口、不创建第三个窗口；关闭后再次点击可重新创建设置窗口且进程继续运行。测试进程已停止，临时 Vault 已删除，未读取或修改用户实际 Vault。
- 修复后全 workspace 165 项自动化测试全部通过，另有 1 项手动性能探针按设计忽略；严格 Clippy、`cargo check -p synapse`、两个 Synapse 包格式检查与 `git diff --check` 均通过。

### 2026-08-14 — Settings 原生标题栏同步全局主题

- 根据产品截图确认 Settings 内容区已经切换为深色，但 macOS 原生标题栏仍保持 Aqua 浅色。根因是 gpui-component `Theme::change` 只更新组件 token 与 GPUI 绘制，不会设置原生 `NSApplication.appearance`；普通系统标题栏因此继续继承系统外观，与应用内显式主题无关。
- 全局主题入口现在先同步 AppKit 应用级外观：System 设置为 `nil` 以恢复系统跟随，Light 使用 `NSAppearanceNameAqua`，Dark 使用 `NSAppearanceNameDarkAqua`，随后再执行既有 GPUI Component 主题、Synapse 自定义 token 和窗口刷新。Settings 标题栏、主窗口原生控件以及系统面板由同一个偏好驱动，不再出现一窗两套主题。
- macOS 原生桥接仅作为 `cfg(target_os = "macos")` 目标依赖接入现有 Cocoa 0.26.0，不影响 Windows/Linux 构建路径，不引入 WebView、HTML/CSS 或 JavaScript。切换动画和 Settings 内容布局没有变化。
- 全 workspace 165 项自动化测试全部通过，另有 1 项手动性能探针按设计忽略；严格 Clippy、两个 Synapse 包格式检查与 `git diff --check` 均通过。按产品要求未运行截图测试。

### 2026-08-14 — 完成待办自动清理与中英文国际化

- Settings 的 Behavior 分组新增 gpui-component 官方 `Switch`：默认关闭；开启后，待办从“未完成”切换为“已完成”时会立即从数据集合和界面中移除并落盘，主待办界面与侧栏快捷列表共用同一业务入口，不存在只改 UI 的分叉逻辑。开启开关不会批量删除历史完成项，历史完成项仍可手动恢复为未完成。
- 自动清理偏好写入 Synapse 原生配置目录的 `todo-auto-clear-completed` 文件，启动时自动恢复；设置窗口通过受控 `checked` 状态实时反映当前值，保存失败会与现有主题、Vault 错误一起显示在设置界面。
- 新增简体中文与 English 两种全局语言状态，默认简体中文并持久化到原生配置目录的 `language` 文件。设置 Appearance 分组新增语言按钮组；切换后同步 gpui-component 的全局 locale，并即时更新设置页、Settings 搜索框、主侧栏、待办/书签工作区、命令面板、页签/文件右键菜单、笔记菜单、编辑器状态提示和已有输入框占位文案。
- Settings 原生窗口初始标题会按当前语言显示 `Synapse 设置` 或 `Synapse Settings`；语言切换会让 Settings 组件以带 locale 的稳定 ID 重新挂载，避免组件内部已创建的搜索输入继续保留旧语言占位符。
- 待办、书签校验错误与主要保存失败信息已接入当前语言；动态空状态、标签筛选结果和“清除已完成 · N”计数文案也分别提供中英文版本。保持纯 Rust + GPUI 架构，没有引入 WebView、HTML/CSS 或 JavaScript。
- 新增自动清理边界回归（新完成项立即删除、历史完成项可恢复）、语言 locale 解析、设置窗口中文标题和布尔偏好兼容解析测试。`cargo test -p synapse` 共 142 项通过、1 项手动性能探针按设计忽略；`cargo clippy -p synapse --all-targets -- -D warnings` 与定向格式化通过。按要求未运行截图测试。

### 2026-08-14 — 语言分段切换与待办自动清理退出动画

- 语言选择不再使用 gpui-component 的普通描边 ButtonGroup，改为与上方主题模式相同的 252×40px 分段容器、4px 内边距、32px 活动指示块和 `SettingsSpring` 420/40/0.5 spring 曲线；简体中文与 English 之间切换时，深色活动块会在两个等宽分段之间连续滑动，文本前景色和 hover 状态与 System / Light / Dark 控件一致。
- 自动清理从“勾选后立即消失”改为明确的三阶段状态机：勾选后先把待办渲染为真实完成态（勾选图标、删除线和弱化文字）并保留 420ms，再用 220ms 将整条待办向右平移 84px，同时把透明度连续降为 0；动画结束后才从待办数据集合移除并再次持久化。
- 主待办列表和左侧快捷待办列表共享相同退出阶段与动画参数，避免同一待办在两个入口表现不一致。移动节点使用 `relative` offset，动画只修改水平位移和透明度，不改背景、不播放入场动画，也不在动画过程中挤压相邻条目。
- 自动清理动画可中断：完成态停留或右移过程中再次点击，会取消对应异步删除任务、把待办恢复为未完成并让退出过渡返回原位；在 Settings 关闭自动清理同样会取消所有尚未完成的退出任务，保留已经显示为完成的待办。手动删除或“清除已完成”会同步清理动画状态，旧定时器不会二次删除。
- 增加语言指示块等宽位置、spring 边界，以及自动清理 420ms 停留 / 220ms 退出 / 84px 位移参数回归。当前 `cargo test -p synapse` 共 144 项通过、1 项手动性能探针按设计忽略；严格 Clippy 与 `git diff --check` 通过。遵循 `make-interfaces-feel-better` 的可中断动画与仅过渡实际变化属性原则，未运行截图测试。

### 2026-08-14 — 通知系统与危险操作统一确认

- 按 `docs/通知系统规范.md` 与 `docs/危险操作确认效果规范.md` 建立并批准 `docs/通知与危险操作实现规格.md`，规格覆盖功能要求、性能/可靠性/键盘/国际化约束、Given/When/Then 验收、错误边界、应用内接口和运行时数据模型；严格规格校验得分 98/100，无错误，唯一提示是本功能按设计不包含 HTTP API。
- 新增统一 `push_alert_notification` 入口：复用 gpui-component `WindowExt::push_notification` 和 `NotificationList` 的右上角定位、关闭、5 秒自动隐藏、队列上限与进出动画；实际通知内容由官方 `Alert::info/success/warning/error` 构建，通知外层去除重复背景、边框、阴影和内边距，避免出现多层卡片。
- 新增 `DangerousAction` 运行时模型和单一 `request_dangerous_action` 入口，集中生成简体中文 / English 标题、对象说明、Danger 确认按钮及成功文案。确认界面使用 gpui-component 原生 `Dialog::confirm`（当前 0.5.1 对 Alert Dialog 的底层实现），禁用遮罩点击关闭与右上角关闭按钮，保留 Escape 取消和 Enter 确认；取消路径不会执行领域操作或显示虚假成功通知。
- 文件树的文件夹删除、笔记移到废纸篓、当前笔记删除，以及待办/书签条目删除、标签删除、标签胶囊移除、标签选择面板取消勾选和批量清除完成项，均已从直接写操作改为先确认再执行。批量完成数为 0 时不打开空确认框；对话框说明显示对象名称或真实数量。
- 待办和书签删除/移除在修改前复制内存工作区，只有持久化成功后才清理选择器、异步状态与错误状态；保存失败立即回滚快照并显示错误 Alert。文件系统删除继续复用 `ShellState::trash_entry` 的脏页签保护、系统废纸篓和错误记录，不绕开现有安全约束。
- 工作区选择器改为窗口绑定的异步任务，使完成回调能够把成功/失败 Alert 投递到触发操作的主窗口或独立 Settings 窗口；成功消息包含实际工作区路径，打开失败和选择器异常显示本地化错误，用户取消保持静默。
- 自动清理完成待办属于用户预先开启的自动策略，明确保留现有可视化延迟和平移淡出流程，不逐条弹出确认框或结果通知。软件更新与撤销功能当前不存在，已在规格中保留未来必须接入同一组件系统的约束。
- 新增危险操作文案双语、批量数量零值和文件夹/笔记类型区分回归。`cargo test -p synapse` 共 147 项通过，另有 1 项既有手动性能探针按设计忽略；`cargo clippy -p synapse --all-targets -- -D warnings`、`cargo check -p synapse` 和定向 rustfmt 均通过。按产品要求未启动软件、未运行截图测试。

### 2026-08-14 — 修复通知与确认对话框创建后不可见

- 根据产品反馈重新从点击事件追踪到 gpui-component 顶层 Root：待办、书签、文件树等入口都已正确调用统一 `request_dangerous_action`，`WindowExt::open_dialog` 也会把记录加入 `Root.active_dialogs`；真正根因是主窗口与独立 Settings 窗口只创建了 `Root::new`，但其内容视图没有调用 `Root::render_dialog_layer` 和 `Root::render_notification_layer`，因此状态已经创建却从未进入 GPUI 元素树，表现为点击删除/移除后完全没有反应。
- 新增共享 `render_component_root_layers`，按 gpui-component 官方根视图约定统一挂载 Sheet、Dialog、Notification 三个浮层；`SynapseApp::render` 与 `SettingsWindow::render` 均在普通内容、上下文菜单和命令面板之后追加这些顶层元素。确认框、遮罩和通知现在会绘制在业务界面上方，且 Settings 中切换工作区产生的通知不会再丢失。
- 使用隔离 `/tmp` Vault 启动真实 GPUI 应用进行无截图端到端验收：通过原生鼠标事件选中临时笔记、打开右键菜单并点击“移到废纸篓”后，磁盘文件保持存在，证明危险操作已被确认框拦截；随后发送 Enter，临时笔记才被移入系统废纸篓，证明确认键、领域删除和关闭流程完整贯通。测试没有读取或修改用户真实 Vault，临时应用实例和 Vault 目录已清理。
- 修复保持原有统一危险操作枚举、双语文案、Danger 按钮、持久化失败回滚与 5 秒 Alert 通知逻辑不变，只补齐缺失的渲染层。`cargo test -p synapse` 共 147 项通过，另有 1 项既有手动性能探针按设计忽略；严格 Clippy 与 `git diff --check` 通过。未运行截图测试。

### 2026-08-14 — 原生斜杠指令面板与笔记链接

- 对照 `ornata-new` 的 `SlashMenu`、`NoteEditor`、`NoteLinkPicker` 和内部链接约定，完整复刻其触发规则与 12 项命令集合：仅在行内起点或空白后匹配 `/查询`，查询支持 Unicode 字母、数字和空格，不会把 URL、普通路径或带标点的文本误判为命令。
- 新增独立的字符偏移命令变换层：正文/H1–H3/四类列表/引用会先规范化当前块前缀再写入目标标记；代码块、分割线和 3×3 表格生成完整可保存 Markdown，并给出可立即继续输入的确定光标位置。所有修改继续走 `ShellState` Rope、增量 Writ 缓冲和原生脏状态链路。
- 菜单使用 208px 宽、32px 行高、8px 圆角、主题 popover、稳定 18px 图标列和 Lucide 图标；最大高度 264px，可滚动并在键盘选中项变化时自动保证条目可见。浅色/深色模式共用全局主题 token，选中和 hover 状态不使用硬编码颜色。
- 菜单跟随 shaped-line 的真实 Unicode 光标坐标，通过原生 anchored/deferred 浮层限制在窗口安全区；剩余空间不足时自动翻到光标上方。打开使用 120ms、4px 的轻量淡入位移，不影响编辑器文本布局或虚拟列表滚动。
- “链接到笔记”不是占位入口：它会保留待替换的斜杠范围，打开带中文 IME 输入的 268px 原生候选面板，排除当前笔记、搜索 Vault 相对路径、显示文件夹路线，并支持上下键、Enter、Escape、hover 与鼠标选择。选中后删除原 `/…` 并插入标准 Markdown 相对链接，路径按 UTF-8 安全编码。
- 新增 7 个官方 Lucide 资源（Heading 1/2/3、List、List Ordered、Text Quote、Table），复用既有 Link、Pilcrow、List Todo、Code、Minus 和 File Text；资源全部嵌入二进制并由现有完整性测试覆盖。
- 新增 6 项斜杠命令回归，覆盖中文触发、URL/路径误触发保护、块前缀转换、代码/分割线/表格光标位置、中文与空格笔记链接编码、中英文命令过滤、当前笔记排除及目录搜索。`cargo test -p synapse` 共 153 项通过，另有 1 项手动性能探针按设计忽略；严格 Clippy 通过。使用隔离 `/tmp` 空 Vault 启动真实 GPUI 构建完成无截图冒烟测试，应用进入事件循环且无启动 panic，随后已停止测试实例；未运行截图测试。

### 2026-08-14 — 斜杠浮层交互与内部笔记链接闭环

- 斜杠菜单和“链接到笔记”候选面板新增独立显隐状态与 generation 隔离：建立后先绘制透明、偏移 4px 的起始帧，16ms 后执行 120ms `EaseOutQuad` 淡入归位；Escape、点击外部、执行命令和完成选择时保留浮层并执行 100ms 反向淡出后再卸载，快速关闭后重开会使旧计时任务失效，不会误关新面板。
- 浮层退场期间保存最后一次真实光标锚点，即使命令已经替换 `/查询` 源码，面板也会从原位置自然消失；斜杠菜单切换到笔记选择器时使用同一锚点交叉过渡，不再瞬间跳变或出现在错误位置。
- 斜杠菜单 12 项 Lucide 图标不再依赖 SVG `currentColor` 的隐式继承：每一行根据选中状态显式使用主题 `foreground` / `muted_foreground`，深色、浅色和 hover/键盘选中状态下均能稳定显示在名称左侧的固定图标列。
- Markdown 链接解析器现在支持渲染态的空光标命中。单击 `[标题](Vault相对路径.md)` 的可见标题时，会解码 UTF-8 百分号路径、补全省略的 `.md`，拒绝 HTTP(S)、邮件、锚点、绝对路径和 `..` 越界路径，并仅在目标确实存在于当前 Vault 笔记快照时调用 `ShellState::select_note` 打开对应页签；普通外部链接和不存在的路径仍保持编辑行为，不会误跳转。
- 新增内部中文路径解析、扩展名补全、外部/越界/缺失目标拒绝及浮层动画时序回归。`cargo test -p synapse --no-fail-fast` 共 155 项通过，另有 1 项手动性能探针按设计忽略；`cargo clippy -p synapse --all-targets -- -D warnings` 与 `git diff --check` 通过。遵循 `make-interfaces-feel-better` 的可中断动画、短退场和显式图标对比度约束，未运行截图测试。

## 下一步

1. 由产品方分别在窄窗口、默认窗口和最大化窗口手动验收标题栏页签、侧栏即时左移、正文边距和长文档滚动体感。
2. 继续缩小正文内容 revision 变化时的 SourceLine 重建范围；光标移动已不再触发 Markdown 全文重新呈现或活动行源码/预览切换。
3. 增加 1k/10k 行 Markdown 持久 Writ 缓冲区命中、局部编辑和虚拟列表状态微基准，建立持续性能预算。
4. 继续实现搜索、会话恢复、更多 Settings 偏好项和 HTML、定义列表等其余扩展 Markdown 组件。
5. 待办 P1（标签选中 pill 滑动、新建标签输入框动画与无边框样式）已实现，等待产品手动验收标签点击体感与输入框展开动画。
