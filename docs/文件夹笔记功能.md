## 文件笔记系统功能规划

注意,这个文档中的功能指的是左侧侧边栏中文件列表的相关功能。

### 功能描述
- 左侧文件区域支持显示文件夹和md格式的文件列表,以及子文件夹等基本信息,上方右侧的新建文件和新建文件夹默认直接创建在用户选中的vault根目录下
- 列表中的文件夹的图标使用lucide的folder,笔记图标使用lucide的file-text
- 列表中的文件夹支持鼠标右键菜单,菜单中的功能初步为:
  - 新建文件夹
  - 在Finder中显示
  - 新建笔记
  - 重命名(由于是在文件夹上的右键菜单,所以这里重命名指的是这个文件夹的名称),删除
  - 以上功能对于任何一个文件夹或者子文件夹都同样支持,这是递归性质的功能
- 每一个笔记文件支持鼠标右键菜单,初步功能支持:
  - 重命名
  - 在Finder中显示
  - 移到废纸篓
- 列表区域的文件夹或者笔记之间要支持拖拽移动,比如某个文件夹移动到某个文件夹下,某个文件移动到某个文件夹下等操作。
- 上述所有右键菜单中的每一个功能都是菜单图标+菜单名称的形式,并且移到废纸篓/删除等操作的图标和菜单名称显示为红色。

---

### P1
- 文件夹或者文件的重命名功能,点击重命名之后,直接在列表区域这个文件夹或者文件的位置将其变成一个可以编辑的输入框形式,用户可以直接输入新的名称回车完成重命名,而不是弹出。
- 移除软件左上角的侧边栏的展开收起图标,将这个图标更换为lucide的panel-left和panel-right ,并且放到编辑器软件界面底部那个固定的地步工具栏的最左侧位置,所以地步工具栏需要常驻显示并且确保左侧面板中底部的设置栏区域的高度和现有的编辑器底部工具栏的高度一致。
- 页签上的鼠标右键菜单的显示位置存在严重的偏移,比如点击左侧的某个页签鼠标右键菜单,它的菜单面板却在编辑器最右侧显示,偏移严重。
- 文件夹或者文件的重命名输入都不支持输入中文啊,只能输入英文字母,什么情况。
- 查看/Users/xuyi/Documents/Synapse/docs/过渡和交互动画规约.md ,完善现有功能的动画效果调整,并且在后续的功能开发中按照这个规约遵守执行。

---

### P2
- 移除左上角的Synpase/notes这个标题文案。
- 完善搜索框的UI效果：搜索框内部左侧显示放大镜图标，默认内容为“Search any...”，右侧显示快捷键徽标。
- 完善编辑器功能,目前编辑器几乎处于不可用状态,比如,鼠标光标不了正常移动,不能通过鼠标移动光标、不能通过鼠标方向键上下进行移动光标。同时也不能输入中文,资料输入字母,并且markdown的内容是源码格式而不是实时预览的形式
- 标准化 Markdown 编辑行为：无序列表、有序列表、任务列表在普通回车后自动续写，空列表项回车退出列表；编辑光标需要周期性闪烁，并在输入或移动后重置为可见。

---

### P3
- 参考开源的markd项目中的引用块的效果,可以看到,它的引用块,渲染之后左侧是一条没有圆角的竖线,并且配色在浅色模式下是深黑色的,深色模式下则是亮白色的。
- 深入研究参考项目中的markdown中的H1到H6的各级标题渲染后的配色情况,我总是感觉我们这个这几级标题的配色太浓太深还是什么情况,总只就是对比看起来和参考项目不一致。
- 处理markdown中的代码块的渲染支持,目前输入代码块语法之后,直接显示语法源码,没有正常渲染代码块,代码也没有高亮效果。
- 参考参考项目的编辑器顶部,左侧是会显示一个当前笔记的面包屑路径效果的,右上角则是由一个菜单图标,点击之后将markdown切换为源码模式,然后有一个更多的菜单图标,点击之后哦有:到处markdown、复制markdown、删除笔记三个操作。
- 修复所有鼠标右键菜单中没有显示菜单图标的问题,比如文件列表中,文件夹或者笔记的鼠标右键菜单面板就只有菜单文本,没有对应的菜单图标了。


---

### P4
- 完善编辑器中对于Mermaid的支持,目前似乎不支持Mermaid图表的渲染处理。
- 左侧侧边栏中文件夹或者文件的鼠标右键的功能体验需要优化,在参考项目中,我鼠标在每一个文件或者文件夹上右键,显示出来的右键菜单面板都会实时准确的显示在对应文件的右下角位置,总之就是位置定位很准确,其次是整个鼠标右键的操作体验流畅丝滑,而且期间鼠标状态就是那个政策的手指状态,但是在我们这个软件中,会变成抓手风格的图标,这应该是拖拽状态才会显示的图标,并且右键菜单的定位也不够准确,总之就是体验很不好,没有参考项目那样平滑自然。你深入研究下参考项目相关实现,然后对我们这个惊喜优化
- 移除右键菜单中的菜单之间的分割线,菜单面板就是一个一体化的效果,不要任何分割线

---

### P5
- 调整软件启动的时候的默认窗口大小为:1809X1332,现在过小了。
- 完善编辑器的数学公式的渲染支持,包括行内公式和公式块
